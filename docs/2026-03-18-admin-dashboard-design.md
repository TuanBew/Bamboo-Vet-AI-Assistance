# Bamboo Vet — Admin SaaS Dashboard Design Spec

**Date:** 2026-03-18
**Status:** Approved (v3 — post spec-review fixes, pass 3)
**Author:** Claude (brainstorming session)
**Scope:** Product B — `/admin/*` dashboard. Product A (chatbot) is untouched.

---

## 1. Overview

Bamboo Vet is a single Next.js monorepo (currently v16, described throughout as "Next.js 15 App Router" — App Router pattern is unchanged) containing two products:

- **Product A** — Customer-facing bilingual AI veterinary chatbot (`/`, `/chat`, `/app`). Already built. No changes.
- **Product B** — Internal admin SaaS dashboard (`/admin/*`). This spec covers Product B entirely.

The admin dashboard is a Vietnamese-only analytics and management platform visible exclusively to users with `is_admin = true`. It provides platform usage analytics, inventory/stock tracking, business customer analytics, user/clinic management, and a pivot-table-heavy data explorer for the veterinary chatbot platform.

---

## 2. Tech Stack

| Layer | Technology |
|---|---|
| Framework | Next.js App Router (v16 in package.json) |
| Styling | Tailwind CSS v4 + shadcn/ui |
| Charts | Recharts *(to be installed — not yet in package.json)* |
| Maps | Leaflet + react-leaflet *(to be installed — not yet in package.json)* |
| Table | @tanstack/react-table *(to be installed)* |
| Export | xlsx (Excel/CSV) + jspdf + jspdf-autotable (PDF) + native window.print() (Print) |
| Database | Supabase (Postgres + Auth + RLS) |
| Auth | Supabase Auth (existing) |
| i18n | Vietnamese only (no toggle) |
| Hosting | Vercel |
| AI/Chat | Not used in admin (RAGflow is Product A only) |

### 2.1 Dependencies to Install

```bash
npm install recharts react-leaflet leaflet @types/leaflet \
  @tanstack/react-table xlsx jspdf jspdf-autotable

npm install --save-dev tsx
```

---

## 3. Database Architecture

### 3.1 New Tables

#### `profiles`
Extends `auth.users`. Auto-populated via `SECURITY DEFINER` trigger on user creation.

```sql
CREATE TABLE profiles (
  id              uuid PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  full_name       text,
  email           text,
  is_admin        boolean NOT NULL DEFAULT false,
  clinic_name     text,
  clinic_type     text,         -- 'phong_kham' | 'nha_thuoc' | 'thu_y' | 'my_pham' | 'cho' | 'khac'
  facility_code   text UNIQUE,  -- Mã cơ sở (unique per clinic, groups staff under one clinic)
  staff_code      text,         -- Mã nhân viên (individual staff identifier within a clinic)
  address         text,
  street          text,
  ward            text,         -- Phường/Xã
  district        text,         -- Quận/Huyện
  province        text,         -- Tỉnh/Thành phố
  region          text,         -- 'Bắc' | 'Trung' | 'Nam'
  zone            text,         -- Vùng kinh tế
  latitude        numeric(9,6),
  longitude       numeric(9,6),
  avatar_url      text,
  created_at      timestamptz NOT NULL DEFAULT now()
);
```

**Trigger (migration: `YYYYMMDD_add_profile_trigger.sql`):**
Must use `SECURITY DEFINER` so the function runs as the owner (bypassing RLS on insert).

```sql
CREATE OR REPLACE FUNCTION handle_new_user()
RETURNS trigger AS $$
BEGIN
  INSERT INTO profiles (id, email)
  VALUES (NEW.id, NEW.email);
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION handle_new_user();
```

**Admin promotion:** Done manually via Supabase SQL editor:
```sql
UPDATE profiles SET is_admin = true WHERE email = 'admin@example.com';
```

**RLS:**
- Users can `SELECT` and `UPDATE` their own row (`id = auth.uid()`)
- Service role can `SELECT` all rows (for admin API)
- No direct client-side `INSERT` (trigger handles it; trigger function is `SECURITY DEFINER`)

---

#### `chat_analytics`
Per-conversation metadata for category analytics. Not auto-generated by the chatbot — populated entirely by the seed script.

```sql
CREATE TABLE chat_analytics (
  id                uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  conversation_id   uuid NOT NULL REFERENCES conversations(id) ON DELETE CASCADE,
  user_id           uuid NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  drug_group        text,   -- 'kháng sinh' | 'vitamin' | 'hormone' | 'vắc-xin' | 'kháng ký sinh trùng' | 'khác'
  animal_type       text,   -- 'trâu bò' | 'lợn' | 'gà' | 'chó mèo' | 'thủy sản' | 'khác'
  query_type        text,   -- 'chẩn đoán' | 'điều trị' | 'phòng bệnh' | 'liều lượng' | 'khác'
  created_at        timestamptz NOT NULL DEFAULT now()
);
```

**RLS:** Service role only. No client-side access.

**Note:** Because `chat_analytics` rows are seeded (not auto-generated), only seeded conversations will appear in analytics views. Real user conversations from Product A (before seeding) will not appear in analytics — this is by design for the internal tool.

---

#### `kb_documents`
Knowledge base document registry. Mirrors RAGflow KB structure but lives in Supabase for analytics. Fully seeded.

```sql
CREATE TABLE kb_documents (
  id              uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  doc_code        text NOT NULL,
  doc_name        text NOT NULL,
  chunk_count     integer NOT NULL DEFAULT 0,
  doc_type        text,   -- 'PDF' | 'DOCX' | 'TXT'
  category        text,
  drug_group      text,
  source          text,   -- 'Bộ NN&PTNT' | 'WHO' | 'Internal' | etc.
  relevance_score numeric(4,3),
  status          text NOT NULL DEFAULT 'active',  -- 'active' | 'draft' | 'archived'
  created_at      timestamptz NOT NULL DEFAULT now()
);
```

**RLS:** Service role only.

---

#### `suppliers`
Veterinary medicine distributors (NPP — Nhà phân phối). Used as the filter dimension on the Nhập hàng page.

```sql
CREATE TABLE suppliers (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  supplier_code text NOT NULL UNIQUE,  -- 'NPP001' ... 'NPP005'
  supplier_name text NOT NULL,         -- Full Vietnamese name
  province      text,
  created_at    timestamptz NOT NULL DEFAULT now()
);
```

**Seed:** 5 supplier rows. Examples: "Công ty TNHH Phân phối Thú y Miền Bắc", "Công ty CP Dược Thú y Trung Nam", etc.

**RLS:** Service role only.

---

#### `products`
Veterinary medicine product catalog. Seeded from `samples/Danh_muc_san_pham_FULL.xlsx` (62 products).

```sql
CREATE TABLE products (
  id               uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  product_code     text NOT NULL UNIQUE,  -- e.g. 'MEGA-BIO', 'DOKSIVIL'
  product_name     text NOT NULL,
  product_group    text NOT NULL,         -- Nhóm (4 groups from XLSX)
  classification   text NOT NULL,         -- Phân loại: 'TABS' | 'Kháng sinh' | 'Cầu trùng, ký sinh trùng' | etc.
  packaging        text,                  -- Quy cách: '1Kg' | 'Chai 1 lít' | etc.
  manufacturer     text,                  -- Short name: 'Megavet' | 'TOPCIN' | 'VILSAN' | etc.
  unit_price       numeric(12,0) NOT NULL, -- Generated: ranges by product group (see §9)
  created_at       timestamptz NOT NULL DEFAULT now()
);
```

**Product groups (4):**
- `NHÓM SẢN PHẨM BỔ TRỢ, BỔ SUNG DINH DƯỠNG` (42 products)
- `NHÓM SẢN PHẨM HỖ TRỢ PHÒNG VÀ ĐIỀU TRỊ` (6 products)
- `NHÓM SẢN PHẨM BỘT LĂN, SÁT TRÙNG CHUỒNG TRẠI, XỬ LÝ CHẤT THẢI CHĂN NUÔI` (7 products)
- `NHÓM SẢN PHẨM ĐIỀU TRỊ KÝ SINH TRÙNG, CẦU TRÙNG` (7 products)

**RLS:** Service role only.

---

#### `purchase_orders`
Purchase order headers — one row per order placed with a supplier.

```sql
CREATE TABLE purchase_orders (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  order_code    text NOT NULL UNIQUE,  -- 'CTT000001', 'CTT000002', ... sequential
  order_date    date NOT NULL,
  supplier_id   uuid NOT NULL REFERENCES suppliers(id) ON DELETE RESTRICT,
  total_amount  numeric(15,0) NOT NULL,  -- Sum of all line items
  total_promo_qty integer NOT NULL DEFAULT 0,
  created_at    timestamptz NOT NULL DEFAULT now()
);
```

**RLS:** Service role only.

---

#### `purchase_order_items`
Line items for each purchase order (one row per product in the order).

```sql
CREATE TABLE purchase_order_items (
  id          uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  order_id    uuid NOT NULL REFERENCES purchase_orders(id) ON DELETE CASCADE,
  product_id  uuid NOT NULL REFERENCES products(id) ON DELETE RESTRICT,
  quantity    integer NOT NULL,
  promo_qty   integer NOT NULL DEFAULT 0,
  unit_price  numeric(12,0) NOT NULL,
  subtotal    numeric(15,0) GENERATED ALWAYS AS (quantity * unit_price) STORED,
  created_at  timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX ON purchase_order_items (order_id);
CREATE INDEX ON purchase_order_items (product_id);
```

**RLS:** Service role only.

---

### 3.2 Materialized Views

All heavy aggregations are pre-computed in PostgreSQL. API routes execute plain `SELECT` queries — no per-request computation. Views are refreshed via `REFRESH MATERIALIZED VIEW CONCURRENTLY`.

#### `mv_monthly_queries`
Drives monthly time-series charts, sparklines, user pivot tables.

```sql
CREATE MATERIALIZED VIEW mv_monthly_queries AS
SELECT
  ca.user_id,
  EXTRACT(YEAR  FROM c.created_at)::int AS year,
  EXTRACT(MONTH FROM c.created_at)::int AS month,
  COUNT(DISTINCT c.id)                   AS session_count,
  COUNT(m.id)                            AS query_count
FROM conversations c
JOIN messages m      ON m.conversation_id = c.id AND m.role = 'user'
JOIN chat_analytics ca ON ca.conversation_id = c.id
GROUP BY ca.user_id, year, month;

CREATE UNIQUE INDEX ON mv_monthly_queries (user_id, year, month);
```

#### `mv_daily_queries`
Drives the clinic detail modal (day-by-day breakdown). Filtered by `user_id`.

```sql
CREATE MATERIALIZED VIEW mv_daily_queries AS
SELECT
  ca.user_id,
  EXTRACT(YEAR  FROM c.created_at)::int AS year,
  EXTRACT(MONTH FROM c.created_at)::int AS month,
  EXTRACT(DAY   FROM c.created_at)::int AS day,
  COUNT(DISTINCT c.id)                   AS session_count,
  COUNT(m.id)                            AS query_count
FROM conversations c
JOIN messages m       ON m.conversation_id = c.id AND m.role = 'user'
JOIN chat_analytics ca ON ca.conversation_id = c.id
GROUP BY ca.user_id, year, month, day;

CREATE UNIQUE INDEX ON mv_daily_queries (user_id, year, month, day);
```

#### `mv_category_stats`
Drives donut/pie charts. Includes `province` and `clinic_type` so dashboard filters can be applied.

```sql
CREATE MATERIALIZED VIEW mv_category_stats AS
SELECT
  EXTRACT(YEAR  FROM c.created_at)::int AS year,
  EXTRACT(MONTH FROM c.created_at)::int AS month,
  p.province,
  p.clinic_type,
  ca.drug_group,
  ca.animal_type,
  ca.query_type,
  COUNT(*)                               AS count
FROM conversations c
JOIN chat_analytics ca ON ca.conversation_id = c.id
JOIN profiles p        ON p.id = ca.user_id
GROUP BY year, month, p.province, p.clinic_type,
         ca.drug_group, ca.animal_type, ca.query_type;

-- Required for REFRESH CONCURRENTLY
CREATE UNIQUE INDEX ON mv_category_stats (year, month, province, clinic_type, drug_group, animal_type, query_type);
```

#### `mv_dashboard_kpis`
Single-row aggregate snapshot for KPI cards. Uses LEFT JOINs so all conversations are counted regardless of `chat_analytics` presence.

**Note:** This view produces a single aggregate row with no natural unique key, so `REFRESH MATERIALIZED VIEW CONCURRENTLY` is **not possible** for this view. The `refresh-views.ts` script must use plain `REFRESH MATERIALIZED VIEW mv_dashboard_kpis` (without `CONCURRENTLY`) for this view only. All other 3 views use `CONCURRENTLY`.

```sql
CREATE MATERIALIZED VIEW mv_dashboard_kpis AS
SELECT
  COUNT(DISTINCT c.id)                                       AS total_sessions,
  COUNT(m.id) FILTER (WHERE m.role = 'user')                AS total_queries,
  COUNT(DISTINCT c.user_id)                                  AS total_users,
  (SELECT COUNT(*) FROM kb_documents WHERE status = 'active') AS total_documents,
  (SELECT COUNT(*) FROM profiles WHERE is_admin = false)     AS total_staff,
  now()                                                      AS refreshed_at
FROM conversations c
LEFT JOIN messages m ON m.conversation_id = c.id;

-- No unique index — this view cannot use REFRESH CONCURRENTLY (single aggregate row).
-- Use: REFRESH MATERIALIZED VIEW mv_dashboard_kpis;
```

**Refresh strategy:** Script `scripts/refresh-views.ts` refreshes all 4 views:
- `mv_monthly_queries`, `mv_daily_queries`, `mv_category_stats` → `REFRESH MATERIALIZED VIEW CONCURRENTLY`
- `mv_dashboard_kpis` → `REFRESH MATERIALIZED VIEW` (non-concurrent, ~instant for a single row)

Triggered manually via "Làm mới dữ liệu" button in the admin top bar (internal server action, not a public API).

---

## 4. Authentication & Security

### 4.1 Critical Pre-Requisite: Fix Middleware Wiring

**`proxy.ts` is not currently active as Next.js middleware.** Next.js App Router only recognises a middleware file named `middleware.ts` (or `middleware.js`) at the project root. The existing `proxy.ts` exports a function named `proxy` — Next.js ignores it entirely. No middleware runs today.

**Required fix (first task of implementation):**
1. Rename `proxy.ts` → `middleware.ts` at the project root
2. Rename the exported function from `proxy` to `middleware`
3. The `config` export (matcher) stays exactly as-is

After this rename, `lib/supabase/middleware.ts`'s `updateSession()` will be correctly invoked on every non-static request.

---

### 4.2 Admin Role Detection

`is_admin` lives in `profiles.is_admin`. Default `false`. Promoted manually via SQL.

**File to modify for admin guards: `lib/supabase/middleware.ts`** (the `updateSession()` function).

**Login redirect logic (extended `updateSession()`):**

After the existing session refresh, the middleware adds:
1. For `/admin/*` routes: fetch `profiles.is_admin` using **`createServiceClient()`** from `lib/supabase/server.ts` (service role, bypasses RLS — the anon client cannot read other users' `profiles` rows). If no session or `is_admin = false` → redirect to `/login`. If `is_admin = true` → allow.
2. For `/app` and `/app/*` routes: after session is confirmed, check `profiles.is_admin`. If `true` → redirect to `/admin/dashboard`.

**The login flow for admins (two-step redirect):**
```
Admin logs in at /login
  → Supabase session created
  → Existing logic redirects to /app
  → Middleware intercepts /app request
  → createServiceClient() fetches profiles.is_admin = true
  → Redirects to /admin/dashboard
```

**Performance note:** The `is_admin` DB fetch adds ~10–30ms per request on `/admin/*` and `/app` routes. Acceptable for an internal tool.

**Service role client:** Use `createServiceClient()` from `lib/supabase/server.ts` — this is the existing service role client that bypasses RLS.

### 4.3 `requireAdmin()` Utility

Location: `lib/admin/auth.ts`

```typescript
import { NextResponse } from 'next/server'
import { createServiceClient } from '@/lib/supabase/server'

type AdminContext = { user: User; profile: Profile }
type AdminResult  = AdminContext | NextResponse

export async function requireAdmin(): Promise<AdminResult> {
  // 1. Get session via createServiceClient()
  // 2. If no session → return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
  // 3. Fetch profiles row: SELECT * FROM profiles WHERE id = user.id
  // 4. If !profile || !profile.is_admin → return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
  // 5. Return { user, profile }
}
```

**Usage pattern in every `/api/admin/*` route:**
```typescript
export async function GET(request: Request) {
  const auth = await requireAdmin()
  if (auth instanceof NextResponse) return auth   // early return on 403

  const { user, profile } = auth
  // proceed — service role client for DB queries
  const db = createServiceClient()
  // ...
}
```

### 4.4 RLS Summary

| Table | anon | authenticated (own row) | service role |
|---|---|---|---|
| `profiles` | ✗ | SELECT + UPDATE | ALL |
| `chat_analytics` | ✗ | ✗ | ALL |
| `kb_documents` | ✗ | ✗ | ALL |
| `conversations` | ✗ | ALL (existing) | ALL |
| `messages` | ✗ | ALL (existing) | ALL |
| Materialized views | ✗ | ✗ | SELECT |

### 4.5 Secrets

No new environment variables are needed for admin. Existing `SUPABASE_SERVICE_ROLE_KEY` is reused. `.env.local` remains git-ignored.

---

## 5. API Routes

All routes under `/api/admin/*`. All protected by `requireAdmin()`. All return JSON. All use the service role Supabase client.

### `GET /api/admin/dashboard`
Query params: `year`, `month`, `province`, `clinic_type`

**KPI filter behaviour:** The 5 KPI cards (`total_queries`, `total_sessions`, etc.) always show **platform-wide totals** — they are not affected by `province`/`clinic_type` filters. Only the charts, category stats, user table, and clinic map respond to filters. This is intentional: the KPIs come from `mv_dashboard_kpis` (no filter dimensions) and serve as the global baseline. The filter bar label should note "Bộ lọc áp dụng cho biểu đồ" (filter applies to charts).

Response shape:
```typescript
{
  kpis: {
    total_queries: number;      // platform-wide, unfiltered
    total_sessions: number;     // platform-wide, unfiltered
    total_users: number;        // platform-wide, unfiltered
    total_documents: number;    // platform-wide, unfiltered
    total_staff: number;        // platform-wide, unfiltered
    refreshed_at: string;
  };
  monthly_series: Array<{
    year: number;
    month: number;
    query_count: number;
    session_count: number;
    is_forecast: boolean;    // true for the 3 projected months
  }>;
  category_stats: {           // filtered by province + clinic_type if provided
    drug_groups:  Array<{ name: string; count: number }>;
    animal_types: Array<{ name: string; count: number }>;
    query_types:  Array<{ name: string; count: number }>;
  };
  top_users: Array<{
    user_id: string;
    full_name: string;
    clinic_name: string;
    clinic_type: string;
    total_queries: number;
    total_sessions: number;
    monthly_sparkline: number[];
  }>;
  clinic_map: Array<{
    user_id: string;
    clinic_name: string;
    clinic_type: string;
    province: string;
    latitude: number;
    longitude: number;
    total_queries: number;
  }>;
  top_clinics: Array<{ clinic_name: string; total_queries: number }>;
}
```

**Forecast computation (API-side):**
The API route computes the 3-month forecast from `mv_monthly_queries` data:
1. Take the last 6 months of real `monthly_series` data (aggregated across all users matching filters)
2. Apply simple linear regression: `y = a + b*x` where `x` is month index (0–5), `y` is `query_count`
3. Extrapolate for months `current+1`, `current+2`, `current+3`
4. Append these 3 rows to `monthly_series` with `is_forecast: true`

Client renders forecast points as a dotted line using Recharts' `strokeDasharray`.

---

### `GET /api/admin/new-activity`
Query params: `year`, `month`

Response shape:
```typescript
{
  kpis: {
    total_new_sessions: number;
    total_new_queries: number;
    total_new_users: number;
    avg_queries_per_session: number;          // total_new_queries / total_new_sessions
    total_new_documents: number;
    avg_session_duration_min: number;
    // avg_session_duration_min computation:
    // For each conversation in the month, compute duration as:
    //   EXTRACT(EPOCH FROM (MAX(m.created_at) - MIN(m.created_at))) / 60
    // Then average these durations across all conversations in the month.
    // Conversations with only 1 message count as 1 minute.
    // Computed via raw SQL in the API route (not a materialized view).
  };
  daily_query_volume: Array<{ day: number; query_count: number }>;
  daily_sessions:     Array<{ day: number; session_count: number }>;
  recent_sessions: Array<{
    session_code: string;    // first 8 chars of conversation.id uppercased, e.g. "A3F7C219"
    date: string;
    user_name: string;
    query_count: number;
    duration_min: number;   // computed as: EXTRACT(EPOCH FROM (MAX(m.created_at) - MIN(m.created_at)))/60
                            // for all messages in the conversation. Minimum 1 min.
  }>;
  top_questions: Array<{ question_prefix: string; count: number }>;
  // top_questions derivation: take all user messages in the selected month, truncate content
  // to first 60 characters, group by this prefix, count occurrences, return top 10 by count.
  // Computed from `messages` table directly (not a materialized view — dataset is small per month).
  category_stats: {
    drug_groups:  Array<{ name: string; count: number }>;
    animal_types: Array<{ name: string; count: number }>;
    query_types:  Array<{ name: string; count: number }>;
  };
}
```

---

### `GET /api/admin/ton-kho`
Query params: `snapshot_date` (YYYY-MM-DD, default = today), `nhom` (product group filter), `phan_loai` (classification/brand filter), `search`, `page`, `page_size`

Response shape:
```typescript
{
  kpis: {
    total_value: number;         // SUM(qty * unit_price) for all products in snapshot
    total_qty: number;           // SUM(qty) for all products in snapshot
    sku_in_stock: number;        // COUNT of products with qty > 0
    total_sku: number;           // total product count
  };
  value_by_nhom:     Array<{ name: string; value: number }>;  // horizontal bar chart data
  value_by_phan_loai: Array<{ name: string; value: number }>;
  value_by_nganh_hang: Array<{ name: string; value: number }>; // donut chart data
  qty_by_nhom:       Array<{ name: string; value: number }>;
  qty_by_phan_loai:  Array<{ name: string; value: number }>;
  qty_by_nganh_hang: Array<{ name: string; value: number }>;
  products: {
    data: Array<{
      product_code: string;
      product_name: string;
      qty: number;
      min_stock: number;        // minimum stock threshold (for low-stock indicator)
      last_import_date: string; // most recent purchase order date for this product
      unit_price: number;
      total_value: number;      // qty * unit_price
    }>;
    total: number;
    page: number;
    page_size: number;
  };
}
```

---

### `GET /api/admin/khach-hang`
Query params: `npp` (NPP filter), `province`, `district`, `customer_type`, `search`, `page`, `page_size`

Response shape:
```typescript
{
  monthly_new_customers: Array<{ month: string; count: number }>;  // "2025-01" format
  customers_by_province:  Array<{ province: string; count: number }>;
  customers_by_district:  Array<{ district: string; count: number }>;
  all_customers_kpis: {
    total_active: number;           // COUNT(*) active customers
    mapped_pct: number;             // % Đã phân tuyến (assigned to route)
    geo_located_pct: number;        // % Đã định vị (has coordinates)
    customer_type_count: number;    // COUNT(DISTINCT customer_type)
  };
  all_customers_breakdown: Array<{
    code: string;    // TH | GSO | PHA | SPS | BTS | OTHER | PLT | WMO
    label: string;   // Vietnamese label for the type
    count: number;
    percentage: number;
  }>;
  buying_customers_kpis: {
    total: number;              // customers with at least one purchase
    mapped_pct: number;
    geo_located_pct: number;
    customer_type_count: number;
  };
  buying_customers_breakdown: Array<{
    code: string;
    label: string;
    count: number;
    pct_of_total: number;      // % of total active customers
    pct_of_active: number;     // % of buying customers who are active
  }>;
  high_value_stores: Array<{   // stores with purchase_value > 300,000 VND
    customer_code: string;
    customer_name: string;
    customer_type: string;
    total_purchase_value: number;
  }>;
}
```

---

### `GET /api/admin/check-users`
Query params: `page`, `page_size`, `search`, `province`, `district`, `clinic_type`

Response shape:
```typescript
{
  map_pins: Array<{
    user_id: string;
    full_name: string;
    clinic_name: string;
    clinic_type: string;
    latitude: number;
    longitude: number;
  }>;
  users: {
    data: Array<{
      user_id: string;
      facility_code: string;
      full_name: string;
      email: string;
      address: string;
      district: string;
      province: string;
      clinic_type: string;
      avatar_url: string | null;
      created_at: string;
      is_geo_located: boolean;
    }>;
    total: number;
    page: number;
    page_size: number;
  };
  monthly_pivot: Array<{
    user_id: string;
    full_name: string;
    months: Record<string, number>;   // key: "2024-01" → "2026-03", value: query_count
  }>;
}
```

---

### `GET /api/admin/users/[userId]/conversations`
**New route** — admin view of a specific user's conversation history. Replaces the claim that the existing `/api/conversations` can be reused (it cannot — it is scoped to the session user).

Uses service role client to bypass RLS.

Response shape:
```typescript
{
  conversations: Array<{
    id: string;
    title: string;
    created_at: string;
    message_count: number;
  }>;
}
```

**"Xem lịch sử" drawer** calls this endpoint to list conversations, then calls `/api/admin/users/[userId]/conversations/[conversationId]/messages` to load messages for a selected conversation.

### `GET /api/admin/users/[userId]/conversations/[conversationId]/messages`
Returns message array for the drawer. Service role only.

```typescript
{
  messages: Array<{
    id: string;
    role: 'user' | 'assistant';
    content: string;
    created_at: string;
  }>;
}
```

---

### `GET /api/admin/check-clinics`
Query params: `year`, `metric` (`queries` | `sessions`), `clinic_type`, `province`, `drug_group`, `search`, `page`, `page_size`

**`[id]` in check-clinics detail = `facility_code`** (unique per clinic — `UNIQUE` constraint on `profiles.facility_code`). A single `facility_code` may have multiple staff users sharing that clinic. The detail endpoint groups by `facility_code`.

Response shape:
```typescript
{
  clinics: {
    data: Array<{
      facility_code: string;       // the row identifier — passed to detail endpoint
      region: string;
      zone: string;
      province: string;
      clinic_name: string;
      monthly_data: Record<string, number>;   // key: "1"–"12", value: query or session count
    }>;
    total: number;
    page: number;
    page_size: number;
  };
}
```

Color coding is computed client-side: `>50` → green, `10–50` → yellow, `1–9` → red, `0` → grey.

---

### `GET /api/admin/check-clinics/[facilityCode]/detail`
`[facilityCode]` = `profiles.facility_code` (unique). Query params: `year`, `month`

Returns all users registered under that `facility_code` and their daily query/session breakdown.

Response shape:
```typescript
{
  clinic_name: string;
  facility_code: string;
  year: number;
  month: number;
  users: Array<{
    user_id: string;
    staff_code: string;
    full_name: string;
    daily_data: Array<{
      day: number;              // 1–31
      query_count: number;
      session_count: number;
    }>;
  }>;
}
```

---

### `GET /api/admin/nhap-hang`
Query params: `npp` (supplier_id), `year`, `month`

Response shape:
```typescript
{
  kpis: {
    total_revenue: number;      // Doanh số — SUM(subtotal) for selected month/npp
    total_quantity: number;     // Số lượng nhập — SUM(quantity)
    total_promo_qty: number;    // SL Khuyến mãi — SUM(promo_qty)
    total_orders: number;       // Đơn nhập — COUNT(DISTINCT purchase_orders.id)
    total_skus: number;         // SKU nhập — COUNT(DISTINCT product_id)
    avg_per_order: number;      // Trung bình/đơn — total_revenue / total_orders
  };
  daily_revenue: Array<{ day: number; revenue: number }>;
  daily_quantity: Array<{ day: number; quantity: number; promo_qty: number }>;
  orders: Array<{
    order_code: string;
    order_date: string;         // 'YYYY/MM/DD'
    total_amount: number;
  }>;
  top_products: Array<{
    product_code: string;
    product_name: string;
    total_revenue: number;
  }>;                           // Top 10, sorted DESC by total_revenue
  by_industry: Array<{ name: string; revenue: number }>;        // by product_group (4 groups)
  by_product_group: Array<{ name: string; revenue: number }>;   // by classification
  by_brand: Array<{ name: string; revenue: number }>;           // by manufacturer
  suppliers: Array<{ id: string; name: string }>;               // All suppliers — for NPP dropdown
}
```

**Filter behaviour:** All fields (KPIs, charts, tables) are filtered by the selected `npp` and `month/year`. Unlike the dashboard, there is no "always-unfiltered" KPI concept — all data is scoped to the active filter.

---

## 6. Export Toolbar Spec (DataTable)

All `DataTable` instances share the same toolbar component. Feature set varies by page:

| Page | Copy | Excel | CSV | PDF | Print | Search |
|---|---|---|---|---|---|---|
| Tồn kho (product list) | ✓ | ✓ | – | – | – | ✓ |
| Khách hàng (customer list) | ✓ | ✓ | – | – | – | ✓ |
| Check Người dùng | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| Check Phòng khám | ✓ | ✓ | – | – | – | ✓ |
| Monthly pivot tables | – | ✓ | – | – | – | ✓ |

**Libraries:**
- **Excel:** `xlsx` library — `XLSX.utils.json_to_sheet()` + `XLSX.writeFile()`
- **CSV:** `xlsx` library — same sheet, write as CSV type
- **PDF:** `jspdf` + `jspdf-autotable` — `doc.autoTable({ head, body })`
- **Print:** `window.print()` with a `@media print` CSS class that hides the sidebar
- **Copy:** `navigator.clipboard.writeText()` with tab-separated values

The `DataTable` component accepts an `exportConfig` prop specifying which buttons to show.

---

## 7. Admin Pages — Full Specification

### 7.1 Admin Layout (`app/admin/layout.tsx`)

- Dark sidebar: `#1a1f2e` background, teal/cyan section labels, white active item highlight
- Collapsible on mobile (hamburger button)
- Top bar: breadcrumb path + "Làm mới dữ liệu" button (server action → `REFRESH MATERIALIZED VIEW CONCURRENTLY` on all 4 views)
- No language toggle (Vietnamese only)
- No RAGflow / AI chat

**Sidebar navigation:**
```
CORE
  🏠 Dashboard              → /admin/dashboard
  📦 Nhập hàng              → /admin/nhap-hang
  📈 Người dùng mới         → /admin/new-activity
  📦 Tồn kho                → /admin/ton-kho
  👥 Khách hàng             → /admin/khach-hang

CHECKED
  🏥 Check Phòng khám       → /admin/check-clinics
  👤 Check Người dùng       → /admin/check-users

OTHER
  ⚙️  Cài đặt               → /admin/settings
```

---

### 7.2 `/admin/dashboard`

**Filter bar:** Province selector | Month/date picker | Clinic type filter | Search

**Tổng quan section:**
- `BarChart`: tổng truy vấn theo năm (2024, 2025, 2026) — grouped bars for queries + sessions
- `AreaChart` with dotted forecast: monthly volume (Jan 2024 → current) + 3-month projection (dotted, `is_forecast: true` points rendered with `strokeDasharray="4 4"`)

**Chỉ số tập trung section:**
- `LineChart`: daily query volume for selected month
- 5 KPI cards: Tổng truy vấn | Phiên chat | Người dùng | Tài liệu KB | Nhân viên
- 6 `PieChart` donuts (3 for queries, 3 for sessions): by drug group | by animal type | by query type
- 4 summary KPI tiles (large number + label)

**Người dùng section:**
- Table: full_name | clinic_name | total | sparkline (12-month line via `SparklineChart`) | sessions | average | days_active
- 2 horizontal stacked `BarChart`s per user row (drug group + query type breakdown)

**Phòng khám section:**
- Leaflet map: color-coded pins by total query volume. Click → tooltip (clinic name + count)
- Top 10 horizontal `BarChart`: clinic name vs query count

---

### 7.3 `/admin/new-activity`

**6 KPI cards** (colored backgrounds — blue, orange, cyan, pink, green, purple):
Tổng phiên mới | Tổng truy vấn | Người dùng mới | TB truy vấn/phiên | Tài liệu mới | TB thời gian phiên

**Charts:**
- `AreaChart`: daily query volume for selected month
- `BarChart`: new sessions per day

**Table — Các phiên mới:**
Columns: Mã phiên | Ngày | Người dùng | Số truy vấn | Thời gian (phút)

**Top 10 câu hỏi phổ biến:**
Horizontal `BarChart`: question prefix (60 chars max) vs count

**3 Donut PieCharts:**
Theo loại động vật | Theo nhóm thuốc | Theo loại truy vấn

---

### 7.3b `/admin/nhap-hang`

**Reference design:** `samples/2_nhap_hang.jpg` — read this image before implementing.

**Filter bar:** NPP dropdown (supplier selector, populated from `suppliers` response) | Month/Year picker | Search button

**6 KPI cards** (colored backgrounds matching reference image):
| # | Label | Value | Color |
|---|---|---|---|
| 1 | Doanh số | total_revenue | Blue |
| 2 | Số lượng nhập | total_quantity | Yellow/gold |
| 3 | SL Khuyến mãi | total_promo_qty | Cyan |
| 4 | Đơn nhập | total_orders | Pink/red |
| 5 | SKU nhập | total_skus | Teal |
| 6 | TB / đơn nhập | avg_per_order | Purple |

**Charts section 1** (2-column grid):
- Left: `"Doanh số nhập tháng MM/YYYY"` — `AreaChart`: X = day (1–31), Y = revenue. Single series `doanh_số`.
- Right: `"Số lượng nhập tháng MM/YYYY"` — `BarChart`: X = day, 2 series: `SL nhập` (quantity) + `SL KM` (promo_qty). Legend below chart.

**Table — "Các đơn đã nhập trong tháng MM/YYYY"** (left half, same row as Top 10 chart):
Columns: `Mã đơn` | `Ngày nhập` | `Thành tiền`
No pagination (show all orders for selected month — typically 2–5 rows).

**Top 10 chart** (right half, same row as orders table):
`"Top 10 sản phẩm nhập tháng MM/YYYY"` — Horizontal `BarChart`: Y = product_name (truncated), X = total_revenue. Single series. Top 10 only.

**Charts section 2** (2-column grid):
- Left: `"Ngành hàng tháng MM/YYYY"` — Donut `PieChart` by `product_group` (4 slices max). Shows revenue share.
- Right: `"Nhóm sản phẩm tháng MM/YYYY"` — Donut `PieChart` by `classification` (up to 9 slices). Shows revenue share.

**Charts section 3:**
`"Thương hiệu tháng MM/YYYY"` — `RadarChart` by `manufacturer`. Each spoke = one manufacturer brand. Value = total_revenue. Use Recharts `<RadarChart>` with `<PolarGrid>`, `<PolarAngleAxis>`, `<Radar>`.

---

### 7.4 `/admin/ton-kho`

**Reference design:** `samples/3_ton_kho.jpg` — read this image before implementing.

**Filter bar:** NPP dropdown | Date picker (snapshot date, default = today) | Brand/Nhóm dropdown | Search button

**3 KPI cards** (colored backgrounds):
| # | Label | Value source | Color |
|---|---|---|---|
| 1 | Tổng giá trị tồn | SUM(qty × unit_price) across all products in snapshot | Blue |
| 2 | Tổng số lượng | SUM(qty) across all products in snapshot | Orange |
| 3 | Số SKU / Tổng SKU | Count of products with qty > 0 / total product count | Teal |

**Charts section 1** (3-column grid, row 1):
- Left: `"Giá trị tồn theo nhóm"` — Horizontal `BarChart`: Y = product_group (Nhóm), X = total_value. Show top groups.
- Center: `"Giá trị tồn theo thương hiệu"` — Horizontal `BarChart`: Y = brand/classification (Phân loại), X = total_value.
- Right: `"Giá trị tồn theo ngành hàng"` — Donut `PieChart`: segments = product category slices, value = total_value.

**Charts section 2** (3-column grid, row 2):
- Left: `"Số lượng tồn theo nhóm"` — Horizontal `BarChart`: Y = product_group, X = qty.
- Center: `"Số lượng tồn theo thương hiệu"` — Horizontal `BarChart`: Y = brand/classification, X = qty.
- Right: `"Số lượng tồn theo ngành hàng"` — Donut `PieChart`: segments = category slices, value = qty.

**Paginated table — "Danh sách sản phẩm tồn kho":**
Columns: Mã sản phẩm | Tên sản phẩm | Số lượng | Tồn min | Ngày nhập mới nhất | Đơn giá | Thành tiền
Export: Copy + Excel. Search bar.

**Data source:** `inventory_snapshots` table joined to `products`. Query uses the selected date (or nearest snapshot ≤ selected date) to return per-product stock levels.

**Product catalog:** 62 products from `samples/Danh_muc_san_pham_FULL.xlsx` — columns: STT, Nhóm (group), TÊN SP (name), Phân loại (brand/classification), Dạng sản phẩm (form), Quy cách (packaging), Nhà sản xuất (manufacturer). These products must be seeded in the `products` table (or a dedicated `catalog_products` table separate from nhap-hang's `products` if their schemas conflict).

---

### 7.5 `/admin/khach-hang`

**Reference design:** `samples/4_khach_hang.jpg` — read this image before implementing.

**Filter bar:** NPP dropdown (top, always visible)

**Charts (3 separate panels):**
- `"Số lượng khách hàng mới theo tháng"` — `LineChart`: X = month ("01-2026"), Y = new_customer_count. Single series.
- `"Số lượng khách hàng theo tỉnh"` — `BarChart`: X = province, Y = count. Single series (Doanh số / count).
- `"Số lượng khách hàng theo huyện"` — Horizontal `BarChart`: Y = district, X = count.

**"Tất cả khách hàng" section** (collapsible, open by default):
- 4 KPI tiles (large colored cards): Khách hàng còn hoạt động | Đã phân tuyến % | Đã định vị % | Số loại cửa hiệu
- Breakdown table (right of KPI tiles): Mã | Icon | Loại cửa hiệu | Số lượng | %
  - Customer types with icon badges: TH (Tạp hóa) | GSO (Bách hóa) | PHA (Nhà thuốc) | SPS (Mẹ & Bé) | BTS (Mỹ phẩm) | OTHER (Khác) | PLT (Phụ liệu tóc) | WMO (Chợ)

**Brand filter button** ("Tất cả thương hiệu") — full-width search/filter row between sections.

**"Khách hàng đang mua hàng" section** (collapsible, open by default):
- 4 KPI tiles: Total customers with orders (can exceed active count — same customer orders multiple times) | Đã phân tuyến % | Đã định vị % | Số loại cửa hiệu
- Breakdown table: Mã | Icon | Loại cửa hiệu | Số lượng | % theo Tổng KH | % theo KH còn hoạt động

**"Số lượng cửa hiệu thực phẩm >300K" section** (collapsible, collapsed by default):
- Table showing stores with purchase value > 300,000 VND. May be empty if no qualifying customers in seed data — show empty state gracefully.

**Data source:** `customers` table (all customers) joined to `customer_purchases` (orders). Customer types mapped to Vietnamese labels and icon badges per type code.

---

### 7.6 `/admin/check-users`

**Full-width Leaflet map:** Clickable pins, color by clinic_type. Popup: full_name + clinic_type.

**Filter bar:** Province | District | Clinic type | Search

**Paginated table:**
Columns: Mã KH | Tên KH | Email | Địa chỉ | Quận/Huyện | Tỉnh | Loại cơ sở | Ảnh cơ sở | Ngày tạo | Định vị | Xem lịch sử
Export: Copy + Excel + CSV + PDF + Print. Search bar. 10 rows default.

**"Xem lịch sử" action:**
- Opens shadcn `Sheet` (right-side drawer)
- Calls `GET /api/admin/users/[userId]/conversations` to list conversations
- Selecting a conversation calls `GET /api/admin/users/[userId]/conversations/[conversationId]/messages`
- Renders messages in read-only chat-style view

**Monthly query pivot table:**
Rows = users, Columns = "2024-01" → "2026-03". Values = query_count.
Export: Excel. Search bar.

---

### 7.7 `/admin/check-clinics`

**Filter bar:** Year | Metric (Truy vấn / Phiên) | Loại cơ sở | Tỉnh | Nhóm thuốc | Search

**Main pivot table:**
Columns: Miền | Vùng | Tỉnh | Mã | Tên | Tháng 1 – Tháng 12
Row identifier: `facility_code`
Cell color (client-side): `>50` → green | `10–50` → yellow | `1–9` → red | `0` → grey
Export: Copy + Excel. Column Visibility toggle. Search. 10 rows default, configurable.

**Clinic detail modal (click any row):**
- `Dialog` component from shadcn/ui, dark background (`bg-gray-900`)
- Title: "Chi tiết theo người dùng — [clinic_name] Tháng [M] Năm [Y]"
- Calls `GET /api/admin/check-clinics/[facilityCode]/detail?year=Y&month=M`
- Rows = staff users in that clinic, Columns = Ngày 1 – Ngày 31
- Each cell: query count (top) + session count (bottom), same color thresholds
- Close button bottom-right

---

### 7.8 `/admin/settings`

Minimal placeholder page. Contains:
- Current admin profile display (name, email, `is_admin` badge)
- "Làm mới dữ liệu" button (duplicate of top bar button — triggers view refresh)
- Timestamp: "Dữ liệu được làm mới lần cuối: [refreshed_at from mv_dashboard_kpis]"

No user management, no role assignment UI (admin promotion is SQL-only by design).

---

## 8. Shared Admin Components

Location: `components/admin/`

| Component | Purpose |
|---|---|
| `AdminSidebar.tsx` | Dark collapsible sidebar with nav groups and active state |
| `KpiCard.tsx` | Colored card: large number + label + optional icon + optional background color prop |
| `SectionHeader.tsx` | Teal header bar: title + collapse toggle (chevron) |
| `DataTable.tsx` | Generic table: @tanstack/react-table + configurable export toolbar |
| `ColorPivotTable.tsx` | Monthly/daily pivot: color-coded cells by value thresholds |
| `ClinicDetailModal.tsx` | Dark Dialog: daily breakdown grid for a clinic |
| `UserHistoryDrawer.tsx` | Right Sheet: conversation list + message viewer |
| `MapView.tsx` | Leaflet wrapper (`dynamic(() => import(...), { ssr: false })`) |
| `SparklineChart.tsx` | Mini Recharts LineChart for table sparklines |
| `FilterBar.tsx` | Controlled filter row: selects + date picker + search input |

---

## 9. Seed Data Strategy

### 9.1 Scope

January 2024 → March 2026 (27 months).

### 9.2 Volume

| Entity | Count |
|---|---|
| `profiles` (non-admin) | 80 |
| `profiles` (admin) | 2 |
| `conversations` | ~4,000 |
| `messages` | ~20,000 (avg 5/conversation) |
| `chat_analytics` | ~4,000 (1:1 with conversations) |
| `kb_documents` | 120 |
| `suppliers` | 5 |
| `products` | 62 (all from XLSX) |
| `purchase_orders` | ~95 (avg 3.5/month × 27 months) |
| `purchase_order_items` | ~760 (avg 8 items/order) |

### 9.2b Nhập hàng Seed Data

**Suppliers (5 rows):**
| Code | Name | Province |
|---|---|---|
| NPP001 | Công ty TNHH Phân phối Thú y Miền Bắc | Hà Nội |
| NPP002 | Công ty CP Dược Thú y Trung Nam | Đà Nẵng |
| NPP003 | Công ty TNHH Thương mại Thú y Phương Nam | TP. Hồ Chí Minh |
| NPP004 | Công ty CP Phân phối Nông nghiệp Việt | Hà Nội |
| NPP005 | Công ty TNHH Dược Thú y Đông Bắc | Hải Phòng |

**Products (62 rows from XLSX):**
Read all 62 products from `samples/Danh_muc_san_pham_FULL.xlsx`. Use columns: STT, Nhóm, TÊN SP, Phân loại, Quy cách, Nhà sản xuất (short form — first company name only, strip address).

**Unit price generation rules (by classification):**
| Classification | Price range (VND/unit) |
|---|---|
| `TABS` (nutritional supplements) | 150,000 – 500,000 |
| `Kháng sinh` (antibiotics) | 200,000 – 800,000 |
| `Cầu trùng, ký sinh trùng` (antiparasitic) | 150,000 – 600,000 |
| `SP Bổ trợ`, `Thuốc bổ` | 100,000 – 400,000 |
| `Hạ sốt - Giảm đau` | 100,000 – 350,000 |
| `Thuốc sát trùng` | 80,000 – 300,000 |
| `SP xử lý CTCN`, `SP rắc chuồng` | 80,000 – 250,000 |

**Purchase orders:**
- Order codes: CTT000001 → CTT000095 (sequential, not padded to year)
- 2–5 orders per month, growing from 2 in 2024 to 4–5 in 2026
- Each order: 5–15 line items from all 62 products (rotate to ensure all products appear at least once over 27 months)
- Promo qty: 30% of orders have promo_qty > 0 per line item (0–3 promo units, smaller products only)
- Supplier rotation: each month, 1–2 suppliers appear. NPP001 and NPP003 are most frequent (40% of orders each)

### 9.3 Geographic Distribution

| Province | Users | Region |
|---|---|---|
| Hà Nội | 15 | Bắc |
| TP. Hồ Chí Minh | 18 | Nam |
| Đà Nẵng | 8 | Trung |
| Bình Dương | 7 | Nam |
| Đồng Nai | 6 | Nam |
| Cần Thơ | 5 | Nam |
| Hải Phòng | 5 | Bắc |
| Nghệ An | 4 | Trung |
| Lâm Đồng | 4 | Trung |
| Khác (mixed) | 8 | mixed |

### 9.4 Clinic Type Distribution

The `clinic_type` values stored in `profiles` must use the enum strings defined in the schema (not display codes). Display labels are derived client-side.

| `clinic_type` value (stored) | Display label | Count |
|---|---|---|
| `phong_kham` | Phòng khám thú y | 28 |
| `nha_thuoc` | Nhà thuốc thú y | 22 |
| `thu_y` | Trại chăn nuôi | 18 |
| `my_pham` | Mỹ phẩm thú y | 8 |
| `khac` | Khác | 4 |

### 9.5 Query Volume Distribution (per month, total platform)

| Period | Avg queries/month |
|---|---|
| 2024 | 80 (low baseline) |
| 2025 H1 | 180 (growth) |
| 2025 H2 | 320 (peak) |
| 2026 Q1 | 280 (seasonal dip → recovery) |

Per-clinic variance: heavy (>50/month) ~15% | medium (10–50) ~50% | light (<10) ~35%

### 9.6 Category Distributions

**Drug groups:** kháng sinh 35% | vitamin 20% | vắc-xin 18% | hormone 12% | kháng ký sinh trùng 10% | khác 5%

**Animal types:** trâu bò 30% | lợn 25% | gà 20% | chó mèo 15% | thủy sản 7% | khác 3%

**Query types:** điều trị 35% | chẩn đoán 28% | liều lượng 20% | phòng bệnh 12% | khác 5%

### 9.7 Knowledge Base

120 documents, 8 drug categories. Types: PDF 60% / DOCX 30% / TXT 10%. Chunks: 10–200 per doc. Relevance: 0.60–0.99.

### 9.8 Files & Scripts

```
data/seeds/
  profiles.ts          — 82 user objects
  conversations.ts     — ~4,000 conversation stubs
  messages.ts          — ~20,000 messages
  chat_analytics.ts    — ~4,000 category rows
  kb_documents.ts      — 120 document records
  suppliers.ts         — 5 NPP supplier rows
  products.ts          — 62 veterinary medicine products (from XLSX)
  purchase_orders.ts   — ~95 order headers (CTT000001–CTT000095)
  purchase_order_items.ts — ~760 line items

scripts/
  seed.ts              — idempotent upsert runner (checks profiles count before inserting)
  refresh-views.ts     — REFRESH MATERIALIZED VIEW CONCURRENTLY for all 4 views
```

**Run:** `npx tsx scripts/seed.ts && npx tsx scripts/refresh-views.ts`

---

## 10. File Structure

```
app/
  admin/
    layout.tsx
    dashboard/page.tsx
    nhap-hang/page.tsx
    new-activity/page.tsx
    ton-kho/page.tsx
    khach-hang/page.tsx
    check-users/page.tsx
    check-clinics/page.tsx
    settings/page.tsx
  api/
    admin/
      dashboard/route.ts
      new-activity/route.ts
      nhap-hang/route.ts
      knowledge-base/route.ts
      users/
        route.ts
        [userId]/
          conversations/
            route.ts
            [conversationId]/
              messages/route.ts
      check-users/route.ts
      check-clinics/
        route.ts
        [facilityCode]/
          detail/route.ts

components/
  admin/
    AdminSidebar.tsx
    KpiCard.tsx
    SectionHeader.tsx
    DataTable.tsx
    ColorPivotTable.tsx
    ClinicDetailModal.tsx
    UserHistoryDrawer.tsx
    MapView.tsx
    SparklineChart.tsx
    FilterBar.tsx

middleware.ts                        — RENAMED from proxy.ts; export renamed proxy→middleware
lib/
  admin/
    auth.ts                         — requireAdmin() utility (uses createServiceClient)
    forecast.ts                     — linear regression helper for forecast series

supabase/
  migrations/
    YYYYMMDD_add_profiles.sql
    YYYYMMDD_add_chat_analytics.sql
    YYYYMMDD_add_kb_documents.sql
    YYYYMMDD_add_materialized_views.sql
    YYYYMMDD_add_profile_trigger.sql

data/seeds/
  profiles.ts
  conversations.ts
  messages.ts
  chat_analytics.ts
  kb_documents.ts

scripts/
  seed.ts
  refresh-views.ts
```

---

## 11. Out of Scope

- Stripe / payments / usage limits
- Mobile app
- Multi-tenant clinic admin accounts
- File/image uploads in chat
- Voice input
- Two-factor authentication
- Audit logging
- External data integrations
- RAGflow / AI chat in admin
- English language support in admin
- Real-time data (materialized views are batch-refreshed)
- Notification system
- Admin-UI role promotion (SQL only)

---

## 12. Decisions Log

| Decision | Choice | Rationale |
|---|---|---|
| Admin role storage | `profiles.is_admin` | Standard Supabase pattern, RLS-compatible, auto-created by SECURITY DEFINER trigger |
| Data aggregation | PostgreSQL materialized views | Fast reads via plain SELECT; no per-request computation |
| Category filter on dashboard | `mv_category_stats` includes `province` + `clinic_type` columns | Enables server-side filtering without raw table joins on request |
| KPI counts | LEFT JOIN (all conversations) | INNER JOIN on `chat_analytics` would silently exclude real user conversations |
| Forecast algorithm | Server-side linear regression, last 6 months | Deterministic, simple, no ML dependency; result included in API response |
| AI/RAGflow in admin | None | Analytics-only tool; AI chat is Product A only |
| Admin language | Vietnamese only | Internal tool, all operators are Vietnamese |
| "Xem lịch sử" endpoint | New `/api/admin/users/[userId]/conversations` routes | Existing route is user-scoped (RLS); admin needs service-role bypass |
| Clinic pivot row key | `facility_code` (UNIQUE) | Clinics group multiple staff; `user_id` would create duplicate clinic rows |
| Export libraries | `xlsx` + `jspdf` + `jspdf-autotable` | Standard, well-maintained, tree-shakeable |
| Middleware wiring | Rename `proxy.ts` → `middleware.ts`, rename export `proxy` → `middleware` | Next.js only runs root `middleware.ts`; `proxy.ts` is currently ignored entirely |
| Service role function | `createServiceClient()` from `lib/supabase/server.ts` | This is the actual function name in the codebase — not `createServiceRoleClient()` |
| Middleware logic file | `lib/supabase/middleware.ts` (`updateSession`) | Actual guard logic goes here after proxy→middleware rename |
| Admin login redirect | Two-hop: `/login` → `/app` → `/admin/dashboard` | Reuses existing post-login redirect; middleware intercepts `/app` for admins |
| KPI filter scope | Platform-wide totals only (unfiltered) | `mv_dashboard_kpis` has no filter dimensions; filter bar applies to charts only |
| Trigger SECURITY DEFINER | Required | RLS on `profiles` blocks trigger insert otherwise |
| `/admin/settings` | Minimal: profile display + refresh button | Internal tool needs no self-serve role management |
| Nhập hàng route | Keep `/admin/nhap-hang` (Vietnamese) | Consistent with Vietnamese-only admin identity |
| Nhập hàng sidebar position | Under CORE, after Dashboard | High-traffic page; matches reference image nav order |
| Product pricing | Fixed ranges by classification | Realistic wholesale pricing; TABS 150K–500K, Kháng sinh 200K–800K |
| Purchase order volume | 2–5 orders/month, 5–15 items/order | Matches reference image showing ~2 orders visible for March 2026 |
| Order code format | CTT000001 sequential | Exact match to reference image (CTT000061, CTT000070) |
| NPP (supplier) filter | Yes — 5 supplier names, dropdown filter | Matches reference image filter bar |
| Promo qty (SL Khuyến mãi) | Yes — separate column, 30% of orders | Matches reference image KPI card and bar chart series |
| Nhập hàng KPI filter scope | All KPIs filtered by NPP + month | Unlike dashboard, no "always-unfiltered" KPI concept |
| Radar chart for brands | `<RadarChart>` by manufacturer | Exact match to reference image "Thương hiệu" section |
