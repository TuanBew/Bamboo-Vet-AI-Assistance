---
phase: 07-performance-optimization
plan: 07
type: execute
wave: 2
depends_on: ["07-05", "07-06"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Build succeeds with zero TypeScript errors after all middleware and streaming changes"
    - "Middleware contains no getUser() or profiles DB queries"
    - "All 4 refactored admin pages have Suspense streaming with Loader pattern"
    - "No regression in auth behavior -- admin routes protected, chatbot routes accessible"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete performance optimization works end-to-end: build passes, middleware has no network calls, admin pages stream correctly, and auth still works.

Purpose: Catch any TypeScript errors, broken imports, or auth regressions from the middleware rewrite and Suspense refactor before deploying.

Output: Verified build + human confirmation that pages load fast.
</objective>

<execution_context>
@C:/Users/MSI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MSI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-performance-optimization/07-CONTEXT.md
@.planning/phases/07-performance-optimization/07-05-SUMMARY.md
@.planning/phases/07-performance-optimization/07-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and code audit</name>
  <files></files>
  <read_first>
    - lib/supabase/middleware.ts (verify no getUser/createServiceClient)
    - app/admin/dashboard/page.tsx (verify Suspense + Loader pattern)
    - app/admin/nhap-hang/page.tsx (verify Suspense + Loader pattern)
    - app/admin/ton-kho/page.tsx (verify Suspense + Loader pattern)
    - app/admin/khach-hang/page.tsx (verify Suspense + Loader pattern)
  </read_first>
  <action>
Run the following verification checks:

1. **Build check:** Run `npx next build` and confirm zero errors.

2. **Middleware audit:** Run these grep commands and confirm results:
   - `grep -rn "getUser" lib/supabase/middleware.ts` must return nothing
   - `grep -rn "createServiceClient" lib/supabase/middleware.ts` must return nothing
   - `grep -rn "\.from(" lib/supabase/middleware.ts` must return nothing
   - `grep -rn "getSession" lib/supabase/middleware.ts` must return a match
   - `grep -rn "app_metadata" lib/supabase/middleware.ts` must return a match

3. **Suspense streaming audit:** For each of the 4 pages (dashboard, nhap-hang, ton-kho, khach-hang):
   - Confirm page.tsx does NOT contain `await get` (no blocking data fetch)
   - Confirm page.tsx contains `Suspense` and a `Loader` component
   - Confirm Loader.tsx exists and contains the async data fetch

4. **Migration file audit:**
   - Confirm `supabase/migrations/20260329_010_custom_access_token_hook.sql` exists
   - Confirm it contains `custom_access_token_hook` function
   - Confirm it has `GRANT EXECUTE` to `supabase_auth_admin`

If build fails, fix TypeScript errors in the files from plans 07-05 and 07-06. Common issues:
- Missing imports in Loader files
- Props type mismatch between Loader and Client component
- Stale references to removed functions in page.tsx
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx next build` exits with code 0 (no errors)
    - Build output shows all routes compiled successfully
    - No TypeScript errors in any modified file
  </acceptance_criteria>
  <done>Build passes, all middleware and streaming changes verified via grep audit, migration file confirmed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of page load speed and auth</name>
  <files></files>
  <action>
Present the following verification checklist to the user. The user will manually verify page load speed and auth behavior after applying the SQL migration and registering the JWT hook.
  </action>
  <what-built>
    Middleware rewritten to eliminate 400-1000ms latency (getSession instead of getUser, JWT claim instead of DB query). Admin pages refactored with Suspense streaming (skeleton shows immediately, data streams in).

    NOTE: The SQL migration file has been created but must be applied manually:
    1. Run the migration SQL in Supabase Dashboard > SQL Editor (copy contents of `supabase/migrations/20260329_010_custom_access_token_hook.sql`)
    2. Register the hook in Supabase Dashboard > Authentication > Hooks > Custom Access Token > select `custom_access_token_hook`
    3. After registering, sign out and sign back in (so the JWT refreshes with the new is_admin claim)
  </what-built>
  <how-to-verify>
    1. Apply the SQL migration and register the hook (see instructions above)
    2. Sign out and sign back in as admin
    3. Open DevTools Network tab
    4. Navigate to `/admin/dashboard` -- should load noticeably faster (no 400-1000ms middleware delay)
    5. Check that the skeleton appears briefly before data loads
    6. Navigate to `/app` as a non-admin user -- should load without middleware delay
    7. Navigate to `/admin/dashboard` as a non-admin -- should redirect to `/login`
    8. Check the check-users, check-clinics, check-distributor, check-customers pages still load
    9. Verify no console errors
  </how-to-verify>
  <verify>Visual inspection in browser</verify>
  <done>User confirms pages load fast and auth behavior is correct</done>
  <resume-signal>Type "approved" if pages load fast and auth works, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. Build passes with zero errors
2. User confirms pages load faster
3. User confirms admin auth still works (non-admin redirected)
4. User confirms chatbot /app routes load fast
</verification>

<success_criteria>
- Build passes
- User confirms noticeable performance improvement
- Auth behavior unchanged (admin protected, chatbot accessible)
- SQL migration applied and JWT hook registered
</success_criteria>

<output>
After completion, create `.planning/phases/07-performance-optimization/07-07-SUMMARY.md`
</output>
