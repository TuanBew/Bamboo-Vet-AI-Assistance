---
phase: 01-database-migrations-seed-data
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/seeds/clinics.md
  - data/seeds/profiles.md
  - data/seeds/conversations.md
  - data/seeds/query_events.md
  - data/seeds/kb_documents.md
autonomous: true
requirements:
  - DB-09
  - DB-10
  - DB-11

must_haves:
  truths:
    - "clinics.md contains at least 15 clinics with Vietnamese names, codes, types, provinces, coordinates"
    - "profiles.md contains 82 rows: 80 non-admin + 2 admin, with geographic distribution matching spec"
    - "conversations.md contains at least 50 complete conversation rows spanning Jan 2024 to Mar 2026"
    - "query_events.md contains rows aligned 1:1 with conversations, with category distributions matching spec"
    - "kb_documents.md contains at least 15 documents with 8 drug categories, 3 doc types, Vietnamese names"
    - "All seed files use markdown table format parseable by a simple regex-based parser"
    - "Clinic type distribution across profiles matches: phong_kham 28, nha_thuoc 22, thu_y 18, my_pham 8, khac 4"
    - "Clinic activity has variance: some >50 queries/month (green), some 10-50 (yellow), some <10 (red)"
  artifacts:
    - path: "data/seeds/clinics.md"
      provides: "Clinic seed data"
      contains: "| id |"
      min_lines: 17
    - path: "data/seeds/profiles.md"
      provides: "User profile seed data"
      contains: "| id |"
      min_lines: 84
    - path: "data/seeds/conversations.md"
      provides: "Conversation seed data"
      contains: "| id |"
      min_lines: 52
    - path: "data/seeds/query_events.md"
      provides: "Query event seed data"
      contains: "| id |"
      min_lines: 52
    - path: "data/seeds/kb_documents.md"
      provides: "KB document seed data"
      contains: "| doc_code |"
      min_lines: 17
  key_links:
    - from: "data/seeds/profiles.md"
      to: "data/seeds/clinics.md"
      via: "clinic_id column references clinic id values"
      pattern: "clinic_id"
    - from: "data/seeds/query_events.md"
      to: "data/seeds/conversations.md"
      via: "conversation_id column references conversation id values"
      pattern: "conversation_id"
    - from: "data/seeds/conversations.md"
      to: "data/seeds/profiles.md"
      via: "user_id column references profile id values"
      pattern: "user_id"
---

<objective>
Create all 5 seed data files as markdown tables with complete, realistic Vietnamese data covering 27 months (Jan 2024 - Mar 2026).

Purpose: Provide the raw data that the seed script (Plan 03) will parse and insert into the database. Every chart, KPI, map pin, and color-coded cell in the admin dashboard must have data to render.
Output: 5 markdown files in `data/seeds/`
</objective>

<execution_context>
@C:/Users/MSI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MSI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-migrations-seed-data/01-CONTEXT.md
@docs/2026-03-18-admin-dashboard-design.md (sections 9.1-9.7 for distributions)

<interfaces>
<!-- Schema columns each seed file must match -->

clinics table columns: id (uuid), name (text), code (text UNIQUE), type (text), province (text), district (text), address (text), lat (numeric 9,6), lng (numeric 9,6), created_at (timestamptz)

profiles table columns: id (uuid), full_name (text), email (text), is_admin (boolean), clinic_id (uuid FK clinics), province (text), district (text), lat (numeric 9,6), lng (numeric 9,6), user_type (text), created_at (timestamptz)

conversations table columns (existing): id (uuid), user_id (uuid FK auth.users), title (text), created_at (timestamptz), updated_at (timestamptz)

query_events table columns: id (uuid), user_id (uuid FK auth.users), conversation_id (uuid FK conversations UNIQUE), clinic_id (uuid FK clinics), drug_category (text), animal_type (text), query_type (text), response_time_ms (integer), created_at (timestamptz)

kb_documents table columns: id (uuid), doc_code (text UNIQUE), doc_name (text), chunk_count (integer), doc_type (text), category (text), drug_group (text), source (text), relevance_score (numeric 4,3), status (text), created_at (timestamptz)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create clinics.md seed data</name>
  <files>data/seeds/clinics.md</files>
  <read_first>
    .planning/phases/01-database-migrations-seed-data/01-CONTEXT.md,
    supabase/migrations/20260318_001_create_clinics.sql
  </read_first>
  <action>
Create directory `data/seeds/` if it does not exist.

Create `data/seeds/clinics.md` containing a markdown table with at least 15 clinic rows. Each row must have ALL columns filled — no empty cells, no placeholders, no "etc.".

**Column headers (first row of table):**
`| id | name | code | type | province | district | address | lat | lng | created_at |`

**Requirements:**
- Each `id` is a valid UUID v4 (generate unique values, e.g., `a1b2c3d4-...`)
- Each `code` is unique (e.g., `PK-HN-001`, `NT-HCM-001`, `TY-DN-001`)
- `type` values must be one of: `phong_kham`, `nha_thuoc`, `thu_y`, `my_pham`, `khac`
- Type distribution across clinics: at least 4 phong_kham, 3 nha_thuoc, 3 thu_y, 2 my_pham, 1 khac (proportional to user counts)
- `province` values must match the geographic distribution: Ha Noi, TP. Ho Chi Minh, Da Nang, Binh Duong, Dong Nai, Can Tho, Hai Phong, Nghe An, Lam Dong, plus others
- Vietnamese clinic names (e.g., "Phong kham thu y Thanh Xuan", "Nha thuoc thu y Minh Phat")
- Use Vietnamese diacritics in names: "Phong kham thu y Thanh Xuan" etc. (but keep province/district names consistent)
- `lat`/`lng` must be realistic Vietnamese coordinates (lat: 10-21, lng: 105-109)
- `created_at` timestamps between 2023-06-01 and 2024-01-15 (clinics existed before data collection started)
- Minimum 15 rows, aim for 18-20 to ensure good geographic coverage

**Activity variance design:** Some clinics will be assigned many users (heavy activity), some moderate, some few users. This is controlled by how many profiles reference each clinic_id. Design clinic count per province to support the profile distribution:
- Ha Noi: 2-3 clinics (to hold 15 users)
- TP.HCM: 3-4 clinics (to hold 18 users)
- Da Nang: 1-2 clinics (to hold 8 users)
- Binh Duong: 1-2 clinics
- Dong Nai: 1 clinic
- Can Tho: 1 clinic
- Hai Phong: 1 clinic
- Nghe An: 1 clinic
- Lam Dong: 1 clinic
- Others: 2-3 clinics

Write every single row completely. Do NOT truncate. Do NOT use "..." or "etc."
  </action>
  <verify>
    <automated>test -f data/seeds/clinics.md && grep -c "^|" data/seeds/clinics.md</automated>
  </verify>
  <acceptance_criteria>
    - data/seeds/clinics.md exists
    - File contains a markdown table header row with columns: id, name, code, type, province, district, address, lat, lng, created_at
    - File has at least 15 data rows (not counting header and separator rows)
    - Every row has all 10 columns filled (no empty cells)
    - `type` values are only from: phong_kham, nha_thuoc, thu_y, my_pham, khac
    - `code` values are unique across all rows
    - `lat` values are between 10 and 22; `lng` values are between 104 and 110
    - No placeholder text like "...", "etc.", "TBD"
  </acceptance_criteria>
  <done>clinics.md exists with at least 15 complete clinic rows with Vietnamese names, realistic coordinates, and proper type distribution</done>
</task>

<task type="auto">
  <name>Task 2: Create profiles.md seed data</name>
  <files>data/seeds/profiles.md</files>
  <read_first>
    data/seeds/clinics.md,
    .planning/phases/01-database-migrations-seed-data/01-CONTEXT.md
  </read_first>
  <action>
Create `data/seeds/profiles.md` containing a markdown table with exactly 82 rows (80 non-admin + 2 admin).

**Column headers:**
`| id | full_name | email | is_admin | clinic_id | province | district | lat | lng | user_type | created_at |`

**Requirements:**
- Each `id` is a unique UUID v4
- `full_name` uses Vietnamese names with diacritics (e.g., "Nguyen Thi Hoa", "Tran Van Minh", "Le Hoang Nam")
- `email` format: firstname.lastname@example.com or similar realistic patterns
- Exactly 2 rows have `is_admin = true`; remaining 80 have `is_admin = false`
- `clinic_id` references a valid UUID from clinics.md (read clinics.md first to get the actual UUIDs)
- Multiple profiles can share the same clinic_id (staff at same clinic)

**Geographic distribution (MANDATORY — match exactly):**
| Province | User Count |
|----------|-----------|
| Ha Noi | 15 |
| TP. Ho Chi Minh | 18 |
| Da Nang | 8 |
| Binh Duong | 7 |
| Dong Nai | 6 |
| Can Tho | 5 |
| Hai Phong | 5 |
| Nghe An | 4 |
| Lam Dong | 4 |
| Others (mixed) | 8 |

**Clinic type distribution (across all 80 non-admin profiles):**
| clinic_type (via clinic) | Count |
|--------------------------|-------|
| phong_kham | 28 |
| nha_thuoc | 22 |
| thu_y | 18 |
| my_pham | 8 |
| khac | 4 |

The `user_type` column should reflect the clinic type of the assigned clinic. Use values like: `nhan_vien` (staff), `quan_ly` (manager), `bac_si` (vet doctor), `duoc_si` (pharmacist).

- `lat`/`lng` should be near the assigned clinic's coordinates (within ~0.05 degrees)
- `created_at` timestamps spread between 2023-12-01 and 2025-06-30 (some early adopters, most joined in 2024)
- Admin accounts should have created_at in early 2024

Write ALL 82 rows completely. Every cell filled. No truncation. No placeholders.
  </action>
  <verify>
    <automated>test -f data/seeds/profiles.md && grep -c "^|" data/seeds/profiles.md</automated>
  </verify>
  <acceptance_criteria>
    - data/seeds/profiles.md exists
    - File contains exactly 82 data rows (grep count of rows starting with | minus 2 for header+separator = 82)
    - Exactly 2 rows contain "true" in the is_admin column
    - Every row has all 11 columns filled
    - clinic_id values in profiles.md match id values that exist in clinics.md
    - No placeholder text like "...", "etc.", "TBD"
    - Province distribution: grep for "Ha Noi" returns 15 data rows, "TP. Ho Chi Minh" returns 18
  </acceptance_criteria>
  <done>profiles.md exists with exactly 82 complete rows (80 non-admin + 2 admin) matching the required geographic and clinic type distributions</done>
</task>

<task type="auto">
  <name>Task 3: Create conversations.md and query_events.md seed data</name>
  <files>data/seeds/conversations.md, data/seeds/query_events.md</files>
  <read_first>
    data/seeds/profiles.md,
    data/seeds/clinics.md,
    .planning/phases/01-database-migrations-seed-data/01-CONTEXT.md
  </read_first>
  <action>
Create TWO files together because they share a 1:1 relationship on conversation_id.

**File 1: `data/seeds/conversations.md`**

Column headers:
`| id | user_id | title | created_at | updated_at |`

**File 2: `data/seeds/query_events.md`**

Column headers:
`| id | user_id | conversation_id | clinic_id | drug_category | animal_type | query_type | response_time_ms | created_at |`

**CRITICAL VOLUME NOTE:** The spec calls for ~4,000 conversations total. Writing 4,000 markdown table rows would be enormous. Instead, create a REPRESENTATIVE SET of at least 200 complete rows that:
1. Cover every month from Jan 2024 through Mar 2026 (27 months)
2. Include conversations from at least 30 different users
3. Show the volume growth curve: fewer rows in early 2024, more in late 2025
4. Include all category distributions

The seed script (Plan 03) will be designed to:
- First insert these 200+ template rows as-is
- Then programmatically generate the remaining ~3,800 rows by cloning patterns from the templates with randomized timestamps and user assignments

**For the 200+ rows you write:**

conversations.md requirements:
- Each `id` is a unique UUID
- `user_id` references a UUID from profiles.md (non-admin profiles only)
- `title` is a Vietnamese conversation title describing the query (e.g., "Lieu luong khang sinh cho ga", "Chan doan benh o lon", "Vac-xin phong benh cho bo")
- `created_at` and `updated_at` timestamps spread across Jan 2024 - Mar 2026
- Volume curve: ~3-4 rows/month in early 2024, ~8-10 rows/month in mid-2025, ~6-8 rows/month in early 2026
- Distribute across at least 30 different user_ids

query_events.md requirements:
- Each `id` is a unique UUID
- `user_id` matches the corresponding conversation's user_id
- `conversation_id` matches the corresponding conversation's id (UNIQUE — one query_event per conversation)
- `clinic_id` matches the clinic assigned to that user in profiles.md
- `drug_category` values: khang_sinh (35%), vitamin (20%), vac_xin (18%), hormone (12%), khang_ky_sinh_trung (10%), khac (5%)
- `animal_type` values: trau_bo (30%), lon (25%), ga (20%), cho_meo (15%), thuy_san (7%), khac (3%)
- `query_type` values: dieu_tri (35%), chan_doan (28%), lieu_luong (20%), phong_benh (12%), khac (5%)
- `response_time_ms`: random between 800-5000
- `created_at` matches the corresponding conversation's created_at

Write ALL rows completely. Every query_events row must have a matching conversations row.
  </action>
  <verify>
    <automated>test -f data/seeds/conversations.md && test -f data/seeds/query_events.md && grep -c "^|" data/seeds/conversations.md && grep -c "^|" data/seeds/query_events.md</automated>
  </verify>
  <acceptance_criteria>
    - data/seeds/conversations.md exists with at least 200 data rows
    - data/seeds/query_events.md exists with at least 200 data rows
    - conversations.md row count equals query_events.md row count (1:1 relationship)
    - conversations.md has columns: id, user_id, title, created_at, updated_at
    - query_events.md has columns: id, user_id, conversation_id, clinic_id, drug_category, animal_type, query_type, response_time_ms, created_at
    - All conversation_id values in query_events.md appear as id values in conversations.md
    - drug_category values are from: khang_sinh, vitamin, vac_xin, hormone, khang_ky_sinh_trung, khac
    - animal_type values are from: trau_bo, lon, ga, cho_meo, thuy_san, khac
    - query_type values are from: dieu_tri, chan_doan, lieu_luong, phong_benh, khac
    - Dates span from 2024-01 through 2026-03 (at least one row in each year)
    - No placeholder text
  </acceptance_criteria>
  <done>conversations.md and query_events.md both exist with at least 200 matching rows covering 27 months of data with correct category distributions and volume growth curve</done>
</task>

<task type="auto">
  <name>Task 4: Create kb_documents.md seed data</name>
  <files>data/seeds/kb_documents.md</files>
  <read_first>
    .planning/phases/01-database-migrations-seed-data/01-CONTEXT.md,
    supabase/migrations/20260318_004_create_kb_documents.sql
  </read_first>
  <action>
Create `data/seeds/kb_documents.md` containing a markdown table with at least 120 document rows.

**Column headers:**
`| doc_code | doc_name | chunk_count | doc_type | category | drug_group | source | relevance_score | status | created_at |`

Note: `id` is auto-generated by the database (DEFAULT gen_random_uuid()), so the seed file uses `doc_code` as the conflict key. Do NOT include an `id` column in this file.

**Requirements:**
- Each `doc_code` is unique (e.g., `KB-001`, `KB-002`, ... `KB-120`)
- `doc_name` uses Vietnamese names describing veterinary documents (e.g., "Huong dan su dung khang sinh trong chan nuoi", "Quy trinh tiem vac-xin cho gia cam")
- `chunk_count`: integer between 10-200
- `doc_type` distribution: PDF 60% (72 rows), DOCX 30% (36 rows), TXT 10% (12 rows)
- `category` values: use 8 categories reflecting veterinary knowledge areas (e.g., "Thu y dai cuong", "Duoc ly", "Dinh duong", "Benh truyen nhiem", "Ky sinh trung", "San khoa", "Ngoai khoa", "Cong nghe sinh hoc")
- `drug_group` values: khang_sinh, vitamin, vac_xin, hormone, khang_ky_sinh_trung, khac, plus null for non-drug documents
- `source` values: "Bo NN&PTNT", "WHO", "FAO", "Internal", "Dai hoc Nong nghiep", "Vien Thu y Quoc gia"
- `relevance_score`: numeric between 0.600 and 0.990 (3 decimal places)
- `status`: mostly "active" (90%), some "draft" (8%), a few "archived" (2%)
- `created_at`: timestamps between 2023-01-01 and 2025-12-31

Write ALL 120 rows completely. Every cell filled. No truncation.

IMPORTANT: This is a large table (120 rows). Write every single row. Do not use "..." or indicate rows should be repeated. Each row must be unique with different doc_name, doc_code, and realistic varied values.
  </action>
  <verify>
    <automated>test -f data/seeds/kb_documents.md && grep -c "^|" data/seeds/kb_documents.md</automated>
  </verify>
  <acceptance_criteria>
    - data/seeds/kb_documents.md exists
    - File contains at least 120 data rows
    - Column headers include: doc_code, doc_name, chunk_count, doc_type, category, drug_group, source, relevance_score, status, created_at
    - doc_code values are unique (no duplicates)
    - doc_type values are only from: PDF, DOCX, TXT
    - status values are only from: active, draft, archived
    - relevance_score values are between 0.600 and 0.990
    - chunk_count values are between 10 and 200
    - No placeholder text like "...", "etc.", "TBD"
    - No id column (database auto-generates uuid)
  </acceptance_criteria>
  <done>kb_documents.md exists with at least 120 complete document rows with Vietnamese names, 8 categories, 3 doc types, and proper distribution of statuses and relevance scores</done>
</task>

</tasks>

<verification>
- All 5 seed files exist in data/seeds/
- clinics.md has at least 15 rows, profiles.md has exactly 82, conversations.md and query_events.md have at least 200 each, kb_documents.md has at least 120
- FK references are consistent: profiles.clinic_id values exist in clinics.id, conversations.user_id values exist in profiles.id, query_events.conversation_id values exist in conversations.id
- Geographic distribution in profiles.md matches spec (15 Ha Noi, 18 HCMC, etc.)
- Category distributions in query_events.md approximate the spec percentages
- All files use markdown table format with `|` column separators
</verification>

<success_criteria>
- 5 complete markdown table files in data/seeds/
- Zero empty cells, zero placeholders, zero truncated rows
- All FK references between files are consistent
- Geographic and category distributions match spec requirements
- Date range covers Jan 2024 through Mar 2026
- Clinic activity variance supports all 3 color states (green >50, yellow 10-50, red <10)
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-migrations-seed-data/01-02-SUMMARY.md`
</output>
