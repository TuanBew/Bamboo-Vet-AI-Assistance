---
phase: 02-admin-shell-role-based-routing
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - components/admin/MapView.tsx
autonomous: true
gap_closure: true
requirements:
  - SHELL-02
  - COMP-04
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - SHELL-01
  - SHELL-03
  - SHELL-04
  - SHELL-05
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-05
  - COMP-06
  - COMP-07

must_haves:
  truths:
    - "REQUIREMENTS.md SHELL-02 no longer mentions hamburger or mobile collapse — reflects desktop-only decision"
    - "MapView.tsx exports MapView via next/dynamic with ssr: false"
    - "MapView.tsx still exports MapPin and MapViewProps types"
    - "next build succeeds with no TypeScript errors on MapView.tsx"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated SHELL-02 requirement text without mobile hamburger clause"
      contains: "SHELL-02"
    - path: "components/admin/MapView.tsx"
      provides: "SSR-safe MapView component stub"
      contains: "ssr: false"
  key_links:
    - from: "components/admin/MapView.tsx"
      to: "next/dynamic"
      via: "dynamic import wrapper"
      pattern: "dynamic\\("
---

<objective>
Close 2 verification gaps from Phase 2 verification report.

Purpose: Align REQUIREMENTS.md with the desktop-only design decision (Gap 1 — SHELL-02) and add the missing next/dynamic SSR-safe wrapper to MapView.tsx (Gap 2 — COMP-04) so Phase 3 can safely import MapView without SSR crashes.

Output: Updated REQUIREMENTS.md + fixed MapView.tsx
</objective>

<execution_context>
@C:/Users/MSI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MSI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-shell-role-based-routing/02-VERIFICATION.md

<interfaces>
<!-- Current MapView.tsx exports (must be preserved): -->
From components/admin/MapView.tsx:
```typescript
export interface MapPin {
  id: string
  latitude: number
  longitude: number
  label: string
  popupContent?: string
  color?: string
}

export interface MapViewProps {
  pins: MapPin[]
  center?: [number, number]
  zoom?: number
  className?: string
}

// Current broken export (plain function, no dynamic wrapper):
export const MapView = MapViewPlaceholder
```

<!-- Current REQUIREMENTS.md SHELL-02 text (line 42): -->
From .planning/REQUIREMENTS.md:
```markdown
- [x] **SHELL-02**: Sidebar has 3 sections (CORE, CHECKED, OTHER) with 7 nav items, teal/cyan section labels, white active item highlight, collapsible on mobile via hamburger
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md SHELL-02 to reflect desktop-only decision</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-admin-shell-role-based-routing/02-CONTEXT.md
  </read_first>
  <action>
In `.planning/REQUIREMENTS.md`, find the SHELL-02 line (line 42):

```
- [x] **SHELL-02**: Sidebar has 3 sections (CORE, CHECKED, OTHER) with 7 nav items, teal/cyan section labels, white active item highlight, collapsible on mobile via hamburger
```

Replace it with:

```
- [x] **SHELL-02**: Sidebar has 3 sections (CORE, CHECKED, OTHER) with 7 nav items, teal/cyan section labels, white active item highlight (desktop-only fixed 240px — no mobile collapse per design decision)
```

This aligns the requirement with the Phase 2 context decision: "Desktop-only internal tool — no mobile compatibility, no hamburger toggle, no responsive collapse."

Do NOT change any other lines in the file.
  </action>
  <verify>
    <automated>grep "SHELL-02" ".planning/REQUIREMENTS.md" | grep -v "hamburger" | grep -q "desktop-only"</automated>
  </verify>
  <acceptance_criteria>
    - SHELL-02 line contains "desktop-only fixed 240px"
    - SHELL-02 line does NOT contain "hamburger"
    - SHELL-02 line does NOT contain "collapsible on mobile"
    - SHELL-02 line still contains "3 sections (CORE, CHECKED, OTHER)"
    - SHELL-02 line still contains "7 nav items"
    - SHELL-02 line still marked [x] complete
    - No other lines in REQUIREMENTS.md are modified
  </acceptance_criteria>
  <done>REQUIREMENTS.md SHELL-02 reflects the desktop-only admin panel design decision; no mention of hamburger or mobile collapse</done>
</task>

<task type="auto">
  <name>Task 2: Add next/dynamic SSR-safe wrapper to MapView.tsx</name>
  <files>components/admin/MapView.tsx</files>
  <read_first>
    - components/admin/MapView.tsx
  </read_first>
  <action>
In `components/admin/MapView.tsx`, make exactly two changes:

1. Add this import at the top of the file (after `'use client'`):
```typescript
import dynamic from 'next/dynamic'
```

2. Replace the last line:
```typescript
export const MapView = MapViewPlaceholder
```
with:
```typescript
export const MapView = dynamic(() => Promise.resolve(MapViewPlaceholder), { ssr: false })
```

Keep the `'use client'` directive. Keep all interfaces (MapPin, MapViewProps) and the MapViewPlaceholder function unchanged. Keep the comment on line 19 unchanged.
  </action>
  <verify>
    <automated>grep -q "import dynamic from 'next/dynamic'" components/admin/MapView.tsx && grep -q "ssr: false" components/admin/MapView.tsx && grep -q "export const MapView = dynamic(" components/admin/MapView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Line 2 (or near top) contains `import dynamic from 'next/dynamic'`
    - Last export line is `export const MapView = dynamic(() => Promise.resolve(MapViewPlaceholder), { ssr: false })`
    - MapPin interface still exported with all 6 fields (id, latitude, longitude, label, popupContent, color)
    - MapViewProps interface still exported with all 4 fields (pins, center, zoom, className)
    - MapViewPlaceholder function body is unchanged
    - `'use client'` directive remains at top of file
    - No other files are modified
  </acceptance_criteria>
  <done>MapView.tsx exports MapView via next/dynamic with ssr: false, preventing SSR crashes when react-leaflet is installed in Phase 3</done>
</task>

</tasks>

<verification>
1. `grep "SHELL-02" .planning/REQUIREMENTS.md` — contains "desktop-only", does NOT contain "hamburger"
2. `grep "ssr: false" components/admin/MapView.tsx` — returns a match
3. `grep "import dynamic" components/admin/MapView.tsx` — returns a match
4. `npx next build` — completes with no TypeScript errors (optional full verification)
</verification>

<success_criteria>
- REQUIREMENTS.md SHELL-02 aligned with desktop-only design decision
- MapView.tsx uses next/dynamic with ssr: false
- Both gaps from 02-VERIFICATION.md are resolved
- No regressions to any other Phase 2 artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-shell-role-based-routing/02-04-SUMMARY.md`
</output>
