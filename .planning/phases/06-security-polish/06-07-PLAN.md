---
phase: 06-security-polish
plan: 07
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified: []
autonomous: false
requirements: [POL-03]

must_haves:
  truths:
    - "createServiceClient does not appear in any .next/static/chunks/ client bundle file"
    - "SUPABASE_SERVICE_ROLE_KEY does not appear in any .next/static/chunks/ client bundle file"
    - "next build completes without TypeScript errors"
  artifacts: []
  key_links:
    - from: ".next/static/chunks/"
      to: "lib/supabase/server.ts"
      via: "Must NOT be linked -- service client must be tree-shaken from client bundle"
      pattern: "createServiceClient"
---

<objective>
Run next build and verify the service role client is absent from the client JavaScript bundle. This is the POL-03 security verification that must happen after all code changes (Plans 01-04) are complete.

Purpose: Prove the service role client boundary is intact after i18n refactoring and PDF handler changes.
Output: Verified build with no service role client in client bundle
</objective>

<execution_context>
@C:/Users/MSI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MSI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-security-polish/06-CONTEXT.md

<interfaces>
<!-- lib/supabase/server.ts exports: -->
export async function createClient()       <!-- uses anon key, safe -->
export function createServiceClient()      <!-- uses service role key, MUST NOT be in client bundle -->

<!-- Service role client usage (all server-side): -->
- app/api/admin/users/[userId]/conversations/route.ts
- app/api/admin/users/[userId]/conversations/[conversationId]/messages/route.ts
- app/admin/settings/page.tsx (Server Component)
- app/admin/_actions/refresh-views.ts (Server Action)
- app/api/conversations/route.ts
- app/api/conversations/[id]/route.ts
- app/api/chat/route.ts
- app/app/layout.tsx (Server Component)
- app/app/conversation/[id]/page.tsx (Server Component)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and verify service role client absence from client bundle</name>
  <files></files>
  <read_first>
    - lib/supabase/server.ts (confirm createServiceClient location)
    - next.config.js (confirm build config)
  </read_first>
  <action>
1. Run `npx next build` to produce a production build.

2. After build completes, verify the client bundle does NOT contain service role references:
```bash
grep -rl "createServiceClient" .next/static/chunks/ 2>/dev/null | wc -l
# Expected: 0

grep -rl "SUPABASE_SERVICE_ROLE_KEY" .next/static/chunks/ 2>/dev/null | wc -l
# Expected: 0

grep -rl "service_role" .next/static/chunks/ 2>/dev/null | wc -l
# Expected: 0
```

3. If any matches are found:
   - Identify the offending file: `grep -rl "createServiceClient" .next/static/chunks/`
   - Trace the import chain to find which client component accidentally imports from server.ts
   - Fix the import (likely by moving the import to a server-only file or adding 'server-only' package)
   - Rebuild and re-verify

4. Also verify the build has no TypeScript errors by checking the build exit code.

5. Report results: "PASS: 0 matches for createServiceClient/SUPABASE_SERVICE_ROLE_KEY in client chunks" or "FAIL: found in [file]"
  </action>
  <verify>
    <automated>cd "D:/importantProjects/WorkSpace_Personal Project_ Agents/Bamboo Vet" && npx next build 2>&1 | tail -5 && MATCHES=$(grep -rl "createServiceClient\|SUPABASE_SERVICE_ROLE_KEY" .next/static/chunks/ 2>/dev/null | wc -l) && echo "Service role in client bundle: $MATCHES matches" && test "$MATCHES" -eq 0 && echo "PASS: Bundle secure"</automated>
  </verify>
  <acceptance_criteria>
    - `npx next build` exits with code 0 (no TypeScript errors)
    - `grep -rl "createServiceClient" .next/static/chunks/` returns 0 matches
    - `grep -rl "SUPABASE_SERVICE_ROLE_KEY" .next/static/chunks/` returns 0 matches
    - `grep -rl "service_role" .next/static/chunks/` returns 0 matches
  </acceptance_criteria>
  <done>Production build succeeds; service role client confirmed absent from client bundle</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of print CSS, PDF diacritics, map tiles, sidebar labels</name>
  <files></files>
  <action>
Present the following verification checklist to the user. The user will manually verify each item in the browser.
  </action>
  <what-built>
Complete Phase 6 security and polish changes:
- POL-01: tsx installed, all packages at correct versions
- POL-02: Print CSS hides sidebar during window.print()
- POL-03: Service role client verified absent from client bundle
- POL-04: CSP updated for Leaflet tile server
- POL-05: Vietnamese font embedded in PDF exports
- i18n dictionary created and all admin components refactored
  </what-built>
  <how-to-verify>
1. **Print CSS test:** Navigate to `/admin/check-users` (or any admin page with a DataTable). Press Ctrl+P (or run window.print() in console). Verify:
   - Sidebar is NOT visible in the print preview
   - Top bar is NOT visible
   - Table content fills the full page width
   - Background is white (not dark)

2. **PDF export test:** On `/admin/check-users`, click the "PDF" export button. Open the downloaded PDF and verify:
   - Vietnamese names render with correct diacritics (e.g., "Nguyen Thi Hoa", not "Nguyen Thi Hoa" or "???")
   - "Ha Noi" renders correctly (not "Ha Noi" or garbled text)

3. **Map tiles test:** Navigate to `/admin/dashboard` or `/admin/check-users`. Verify:
   - Leaflet map tiles load correctly (no grey/broken tiles)
   - Open browser DevTools Console -- no CSP errors about tile.openstreetmap.org

4. **Sidebar diacritics:** Check the admin sidebar. Verify labels now show:
   - "Nhap hang" (with proper Vietnamese diacritics)
   - "Ton kho" (with proper Vietnamese diacritics)
   - "Khach hang" (with proper Vietnamese diacritics)
   - "Check Phong kham" (with proper Vietnamese diacritics)
   - "Cai dat" (with proper Vietnamese diacritics)
  </how-to-verify>
  <verify>Visual inspection in browser</verify>
  <done>User confirms print CSS, PDF diacritics, map tiles, and sidebar labels all work correctly</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- next build exits 0
- 0 matches for createServiceClient in .next/static/chunks/
- Human verification of print CSS, PDF diacritics, map tiles, sidebar labels
</verification>

<success_criteria>
1. Build succeeds with no TypeScript errors
2. Service role client provably absent from client bundle
3. Human verifies print CSS, PDF Vietnamese diacritics, map tiles, and sidebar labels
</success_criteria>

<output>
After completion, create `.planning/phases/06-security-polish/06-07-SUMMARY.md`
</output>
