---
phase: 06-security-polish
plan: 04
type: execute
wave: 2
depends_on: ["06-03"]
files_modified:
  - app/admin/dashboard/DashboardClient.tsx
  - app/admin/nhap-hang/NhapHangClient.tsx
  - app/admin/ton-kho/TonKhoClient.tsx
  - app/admin/khach-hang/KhachHangClient.tsx
  - app/admin/check-customers/CheckCustomersClient.tsx
  - app/admin/check-distributor/CheckDistributorClient.tsx
  - app/admin/check-users/CheckUsersClient.tsx
  - app/admin/check-clinics/CheckClinicsClient.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 8 page client components use VI dictionary for section titles, KPI labels, and chart titles"
    - "No hardcoded Vietnamese strings remain in page client components"
    - "Pages render identically before and after refactor"
  artifacts:
    - path: "app/admin/dashboard/DashboardClient.tsx"
      provides: "Dashboard page with VI.dashboard.* references"
      contains: "VI.dashboard"
    - path: "app/admin/nhap-hang/NhapHangClient.tsx"
      provides: "Nhap Hang page with VI.nhapHang.* references"
      contains: "VI.nhapHang"
    - path: "app/admin/check-users/CheckUsersClient.tsx"
      provides: "Check Users page with VI.checkUsers.* references"
      contains: "VI.checkUsers"
  key_links:
    - from: "app/admin/*/Client.tsx"
      to: "lib/i18n/vietnamese.ts"
      via: "import { VI } from '@/lib/i18n/vietnamese'"
      pattern: "import.*VI.*vietnamese"
---

<objective>
Refactor all 8 admin page client components to use the centralized VI dictionary for their Vietnamese labels, section titles, KPI names, chart titles, and table headers.

Purpose: Complete the i18n extraction across all page-level components for consistency and diacritic correctness.
Output: Updated page client components using VI.* references instead of hardcoded strings
</objective>

<execution_context>
@C:/Users/MSI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MSI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-security-polish/06-CONTEXT.md

<interfaces>
<!-- From lib/i18n/vietnamese.ts (created in 06-03): -->
export const VI = {
  dashboard: { title, overview, focusMetrics, users, clinics, totalSessions, ... },
  nhapHang: { title, totalOrders, totalRevenue, dailyRevenue, ... },
  tonKho: { title, totalValue, totalQty, skuCount, valueByGroup, ... },
  khachHang: { title, newByMonth, byProvince, allCustomers, ... },
  checkUsers: { title, viewHistory, conversationHistory, ... },
  checkClinics: { title, region, zone, province, ... },
  checkCustomers: { title, checkLocation, brandRevenue, ... },
  checkDistributor: { title, columnVisibility, distributorDetail, ... },
  filter: { province, district, clinicType, year, month, ... },
  common: { loading, error, noData, ... },
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor CORE page clients (Dashboard, NhapHang, TonKho, KhachHang)</name>
  <files>app/admin/dashboard/DashboardClient.tsx, app/admin/nhap-hang/NhapHangClient.tsx, app/admin/ton-kho/TonKhoClient.tsx, app/admin/khach-hang/KhachHangClient.tsx</files>
  <read_first>
    - lib/i18n/vietnamese.ts (the dictionary -- understand available keys)
    - app/admin/dashboard/DashboardClient.tsx (find all hardcoded Vietnamese strings: section titles like "Tong quan", KPI labels, chart titles)
    - app/admin/nhap-hang/NhapHangClient.tsx (find all hardcoded Vietnamese strings)
    - app/admin/ton-kho/TonKhoClient.tsx (find all hardcoded Vietnamese strings)
    - app/admin/khach-hang/KhachHangClient.tsx (find all hardcoded Vietnamese strings)
  </read_first>
  <action>
For each of the 4 CORE page client files:

1. Add `import { VI } from '@/lib/i18n/vietnamese'` at the top (after existing imports)
2. Search for ALL hardcoded Vietnamese strings in the file
3. Replace each with the corresponding `VI.*` reference

**Mapping guidance per page:**

**DashboardClient.tsx:**
- Section titles: "Tong quan" -> `VI.dashboard.overview`, "Chi so tap trung" -> `VI.dashboard.focusMetrics`, "Nguoi dung" -> `VI.dashboard.users`, "Phong kham" -> `VI.dashboard.clinics`
- KPI labels: "Tong phien" -> `VI.dashboard.totalSessions`, "Tong cau hoi" -> `VI.dashboard.totalQueries`, etc.
- If strings are passed as props to KpiCard/SectionHeader, replace at the call site

**NhapHangClient.tsx:**
- KPI labels: "Tong don hang" -> `VI.nhapHang.totalOrders`, "Tong doanh thu" -> `VI.nhapHang.totalRevenue`, etc.
- Chart titles: "Doanh thu theo ngay" -> `VI.nhapHang.dailyRevenue`, etc.
- Table header: "Danh sach don hang" -> `VI.nhapHang.orderList`

**TonKhoClient.tsx:**
- KPI labels: "Tong gia tri ton" -> `VI.tonKho.totalValue`, "Tong so luong" -> `VI.tonKho.totalQty`, "So SKU" -> `VI.tonKho.skuCount`
- Chart titles: "Gia tri theo nhom" -> `VI.tonKho.valueByGroup`, etc.
- Table header: "Danh sach san pham ton kho" -> `VI.tonKho.productList`

**KhachHangClient.tsx:**
- Section titles: "Tat ca khach hang" -> `VI.khachHang.allCustomers`, "Khach hang dang mua hang" -> `VI.khachHang.purchasingCustomers`
- KPI labels: "Con hoat dong" -> `VI.khachHang.active`, "Da phan tuyen" -> `VI.khachHang.mapped`, etc.
- Table headers: "Ma" -> `VI.khachHang.typeCode`, "Loai cua hieu" -> `VI.khachHang.typeName`, "So luong" -> `VI.khachHang.count`

**IMPORTANT:** If a string does not have an exact match in the VI dictionary, add the key to `lib/i18n/vietnamese.ts` first, then use it. Keep the dictionary comprehensive.

**IMPORTANT:** Some labels may already lack diacritics (e.g., "Tong quan" without accents). Replace these with the diacritically correct VI.* reference which contains "Tổng quan". This is intentional -- the dictionary is the single source of truth for correct Vietnamese.
  </action>
  <verify>
    <automated>cd "D:/importantProjects/WorkSpace_Personal Project_ Agents/Bamboo Vet" && grep -q "import.*VI.*vietnamese" app/admin/dashboard/DashboardClient.tsx && grep -q "import.*VI.*vietnamese" app/admin/nhap-hang/NhapHangClient.tsx && grep -q "import.*VI.*vietnamese" app/admin/ton-kho/TonKhoClient.tsx && grep -q "import.*VI.*vietnamese" app/admin/khach-hang/KhachHangClient.tsx && echo "CORE pages refactored"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 CORE page clients import VI from '@/lib/i18n/vietnamese'
    - DashboardClient.tsx contains VI.dashboard.* references for section titles and KPI labels
    - NhapHangClient.tsx contains VI.nhapHang.* references
    - TonKhoClient.tsx contains VI.tonKho.* references
    - KhachHangClient.tsx contains VI.khachHang.* references
    - No hardcoded Vietnamese strings remain that have dictionary equivalents
  </acceptance_criteria>
  <done>CORE page clients refactored to use VI dictionary with correct diacritics</done>
</task>

<task type="auto">
  <name>Task 2: Refactor CHECKED page clients (CheckCustomers, CheckDistributor, CheckUsers, CheckClinics)</name>
  <files>app/admin/check-customers/CheckCustomersClient.tsx, app/admin/check-distributor/CheckDistributorClient.tsx, app/admin/check-users/CheckUsersClient.tsx, app/admin/check-clinics/CheckClinicsClient.tsx</files>
  <read_first>
    - lib/i18n/vietnamese.ts (the dictionary -- may have been updated in Task 1)
    - app/admin/check-customers/CheckCustomersClient.tsx (find hardcoded Vietnamese strings)
    - app/admin/check-distributor/CheckDistributorClient.tsx (find hardcoded Vietnamese strings)
    - app/admin/check-users/CheckUsersClient.tsx (find hardcoded Vietnamese strings: "Xem lich su", table headers, drawer titles)
    - app/admin/check-clinics/CheckClinicsClient.tsx (find hardcoded Vietnamese strings: "Mien", "Vung", "Tinh", etc.)
  </read_first>
  <action>
For each of the 4 CHECKED page client files:

1. Add `import { VI } from '@/lib/i18n/vietnamese'` at the top
2. Search for ALL hardcoded Vietnamese strings
3. Replace with VI.* references

**Mapping guidance per page:**

**CheckCustomersClient.tsx:**
- "Check Location" -> `VI.checkCustomers.checkLocation`
- Revenue pivot title -> `VI.checkCustomers.brandRevenue`
- Display programs title -> `VI.checkCustomers.displayPrograms`
- Table column headers (Ma KH, Ten KH, etc.) -- if hardcoded, add keys and replace

**CheckDistributorClient.tsx:**
- Column visibility label -> `VI.checkDistributor.columnVisibility`
- Modal title -> `VI.checkDistributor.distributorDetail`
- "Truoc"/"Tiep theo" -> already handled in ColorPivotTable (Plan 03), but if duplicated here, replace
- Staff/Day headers -> `VI.checkDistributor.staff`, `VI.checkDistributor.day`

**CheckUsersClient.tsx:**
- "Xem lich su" button -> `VI.checkUsers.viewHistory`
- Drawer title "Lich su hoi thoai" -> `VI.checkUsers.conversationHistory`
- Table column headers (Ma KH, Ten KH, Email, etc.) -- add keys if needed
- "Chua co hoi thoai" -> `VI.checkUsers.noConversations`

**CheckClinicsClient.tsx:**
- Pivot headers: "Mien" -> `VI.checkClinics.region`, "Vung" -> `VI.checkClinics.zone`, "Tinh" -> `VI.checkClinics.province`, "Ma" -> `VI.checkClinics.code`, "Ten" -> `VI.checkClinics.name`
- Modal title -> `VI.checkClinics.clinicDetail`
- Month headers "Thang 1"..."Thang 12" -- these may be generated programmatically, leave as-is if dynamic

**IMPORTANT:** If new keys are needed that do not exist in the VI dictionary, add them to `lib/i18n/vietnamese.ts` before using. Keep the dictionary as the single source of truth.
  </action>
  <verify>
    <automated>cd "D:/importantProjects/WorkSpace_Personal Project_ Agents/Bamboo Vet" && grep -q "import.*VI.*vietnamese" app/admin/check-customers/CheckCustomersClient.tsx && grep -q "import.*VI.*vietnamese" app/admin/check-distributor/CheckDistributorClient.tsx && grep -q "import.*VI.*vietnamese" app/admin/check-users/CheckUsersClient.tsx && grep -q "import.*VI.*vietnamese" app/admin/check-clinics/CheckClinicsClient.tsx && echo "CHECKED pages refactored"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 CHECKED page clients import VI from '@/lib/i18n/vietnamese'
    - CheckUsersClient.tsx uses VI.checkUsers.viewHistory instead of hardcoded "Xem lich su"
    - CheckClinicsClient.tsx uses VI.checkClinics.* for pivot column headers
    - CheckDistributorClient.tsx uses VI.checkDistributor.* for modal title and column visibility
    - CheckCustomersClient.tsx uses VI.checkCustomers.* for section titles
    - lib/i18n/vietnamese.ts may have additional keys added during this task
  </acceptance_criteria>
  <done>CHECKED page clients refactored to use VI dictionary with correct diacritics</done>
</task>

</tasks>

<verification>
- All 8 page client files import from `@/lib/i18n/vietnamese`
- `grep -r "import.*VI.*vietnamese" app/admin/` returns 8+ matches (shared components + page clients)
- No obvious hardcoded Vietnamese label strings remain in page clients
</verification>

<success_criteria>
1. All 8 admin page clients import and use the VI dictionary
2. Section titles, KPI labels, chart titles, table headers use VI.* references
3. Vietnamese diacritics are correct throughout (via dictionary values)
4. No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-security-polish/06-04-SUMMARY.md`
</output>
