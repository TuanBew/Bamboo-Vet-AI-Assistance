---
phase: 06-security-polish
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - data/seeds/profiles.ts
  - data/seeds/conversations.ts
  - data/seeds/messages.ts
  - data/seeds/chat_analytics.ts
  - data/seeds/query_events.ts
autonomous: true
requirements: []  # Seed expansion per CONTEXT.md locked decision - Seed Data Completeness (Phase 1 Amendment)

must_haves:
  truths:
    - "Profile generator produces 120-150 users with geographic spread across Vietnamese provinces"
    - "Conversation generator produces 10,000-12,000 conversations with non-linear growth curve"
    - "Message generator produces 50,000-60,000 messages (4-6 per conversation average)"
    - "Chat analytics generator produces 1:1 rows with conversations"
    - "Query events generator matches conversation distribution"
  artifacts:
    - path: "data/seeds/profiles.ts"
      provides: "Deterministic profile generator for 120-150 users"
      exports: ["generateProfiles"]
    - path: "data/seeds/conversations.ts"
      provides: "Deterministic conversation generator for 10K-12K conversations"
      exports: ["generateConversations"]
    - path: "data/seeds/messages.ts"
      provides: "Deterministic message generator for 50K-60K messages"
      exports: ["generateMessages"]
    - path: "data/seeds/chat_analytics.ts"
      provides: "Deterministic chat_analytics generator"
      exports: ["generateChatAnalytics"]
  key_links:
    - from: "data/seeds/conversations.ts"
      to: "data/seeds/profiles.ts"
      via: "References profile user IDs for conversation user_id"
      pattern: "generateProfiles"
    - from: "data/seeds/messages.ts"
      to: "data/seeds/conversations.ts"
      via: "References conversation IDs for message conversation_id"
      pattern: "generateConversations"
---

<objective>
Create TypeScript generator functions for the expanded seed data: profiles (120-150), conversations (10K-12K), messages (50K-60K), chat_analytics (12K), and query_events. These replace the existing markdown-based seed files with deterministic algorithmic generators.

This plan executes seed data expansion as defined in Phase 6 CONTEXT.md (locked decision, Phase 1 Amendment). No formal POL-XX requirement ID, but critical for realistic data verification across all dashboard pages.

Purpose: Expand seed data to cover full 27 months (Jan 2024 - Mar 2026) with realistic non-linear distribution for dashboard visualizations.
Output: 5 TypeScript generator files in data/seeds/
</objective>

<execution_context>
@C:/Users/MSI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MSI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-security-polish/06-CONTEXT.md
@.planning/phases/06-security-polish/06-RESEARCH.md

<interfaces>
<!-- Existing deterministic hash pattern from Phase 04 (used in customer_purchases.ts, inventory_snapshots.ts): -->
function deterministicHash(seed: string): number {
  let hash = 0
  for (let i = 0; i < seed.length; i++) {
    hash = ((hash << 5) - hash + seed.charCodeAt(i)) | 0
  }
  return Math.abs(hash)
}

<!-- Database table schemas (from Phase 01 migrations): -->
profiles: id (uuid), full_name, email, province, district, ward, region, latitude, longitude, clinic_type, facility_code, staff_code, is_admin, created_at
conversations: id (uuid), user_id (uuid FK profiles.id), title, created_at
messages: id (uuid), conversation_id (uuid FK conversations.id), role ('user'|'assistant'), content, created_at
chat_analytics: id (uuid), conversation_id (uuid FK), drug_group, animal_type, query_type, created_at
query_events: id (uuid), user_id, conversation_id, drug_group, animal_type, query_type, province, clinic_type, year, month, day, created_at

<!-- UUID prefix convention from Phase 01: c0a1 for clinics, p0a2 for profiles, cv for conversations, qe for query_events -->

<!-- Growth curve from CONTEXT.md: -->
2024: 60-100 queries/month (ramp)
2025 Q1-Q2: 150-250/month (growth)
2025 Q3-Q4: 350-450/month (peak)
2026 Q1: 200-300/month (seasonal dip)
Daily variance: 20-30% fluctuation, weekday > weekend
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create profile and conversation generators</name>
  <files>data/seeds/profiles.ts, data/seeds/conversations.ts</files>
  <read_first>
    - data/seeds/profiles.md (current 82 profiles -- understand column structure and geographic distribution)
    - data/seeds/conversations.md (current ~210 conversations -- understand column structure)
    - data/seeds/customers.ts (reference for existing generator pattern and deterministicHash function)
    - data/seeds/products.ts (reference for generator export pattern)
    - scripts/seed.ts (understand how generators are consumed -- the seed runner)
  </read_first>
  <action>
**data/seeds/profiles.ts:**

Create a new file (or replace existing if it's a .ts generator) that exports a `generateProfiles()` function returning an array of profile objects. Requirements:

1. Use the same `deterministicHash(seed)` function pattern established in `data/seeds/customers.ts`
2. Generate 130 non-admin profiles + 2 admin profiles = 132 total
3. UUID format: `p0a2XXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX` (pad deterministic hex from index)
4. Geographic distribution across Vietnamese provinces:
   - Ha Noi: 20 users
   - Ho Chi Minh: 25 users
   - Da Nang: 10 users
   - Binh Duong: 8 users
   - Dong Nai: 7 users
   - Hai Phong: 6 users
   - Can Tho: 5 users
   - Long tail: remaining ~49 users spread across 20+ other provinces
5. Clinic types: Phong kham (40%), Nha thuoc (30%), Trai chan nuoi (20%), My pham (7%), Khac (3%)
6. Each profile has: full_name (Vietnamese names), email (derived from name), province, district, ward, region (Bac/Trung/Nam), latitude/longitude (realistic within province bounds), clinic_type, facility_code (FAC-XXXX), staff_code (STF-XXXX), is_admin (false except 2), created_at (spread across Jan 2024 - Dec 2025)
7. Vietnamese name pool: at least 50 family names (Nguyen, Tran, Le, Pham, Hoang, etc.) x 30 given names
8. Export: `export function generateProfiles(): ProfileSeed[]` where ProfileSeed matches the table schema

**data/seeds/conversations.ts:**

Create a generator that exports `generateConversations(profiles: ProfileSeed[])` returning conversation objects.

1. Total: 10,000-12,000 conversations across 27 months (Jan 2024 - Mar 2026)
2. Growth curve function:
   ```
   monthIndex 0-11 (2024): base 60 + (monthIndex/11)*40 -> 60-100/month
   monthIndex 12-17 (2025 Q1-Q2): base 150 + ((monthIndex-12)/5)*100 -> 150-250/month
   monthIndex 18-23 (2025 Q3-Q4): base 350 + ((monthIndex-18)/5)*100 -> 350-450/month
   monthIndex 24-26 (2026 Q1): base 300 - ((monthIndex-24)/2)*50 -> 200-300/month
   ```
3. Apply 20-30% deterministic variance per month using `deterministicHash('vol-YYYY-MM')`
4. Daily distribution within each month: weekdays get 1.3x weight, weekends get 0.7x weight
5. Each conversation assigned to a random (deterministic) user from profiles
6. UUID format: `cvXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX`
7. Title: generated from drug group + animal type template (e.g., "Hoi ve khang sinh cho lon", "Tu van vac-xin cho ga")
8. created_at: specific date + time within the assigned day (work hours 7am-9pm weighted higher)
9. Export: `export function generateConversations(profiles: ProfileSeed[]): ConversationSeed[]`
  </action>
  <verify>
    <automated>cd "D:/importantProjects/WorkSpace_Personal Project_ Agents/Bamboo Vet" && node -e "
const p = await import('file://' + process.cwd().replace(/\\\\/g,'/') + '/data/seeds/profiles.ts');
const profiles = p.generateProfiles();
console.log('Profiles:', profiles.length);
if (profiles.length < 120 || profiles.length > 155) process.exit(1);
const admins = profiles.filter(p => p.is_admin);
if (admins.length !== 2) process.exit(1);
console.log('Admins:', admins.length, '- OK');
" 2>/dev/null || (test -f data/seeds/profiles.ts && grep -q "generateProfiles" data/seeds/profiles.ts && echo "Profile generator exists")</automated>
  </verify>
  <acceptance_criteria>
    - data/seeds/profiles.ts exports `generateProfiles()` function
    - Function returns 120-155 profile objects (130 non-admin + 2 admin)
    - Profiles have UUID format starting with `p0a2`
    - Profiles span provinces: Ha Noi (15+), HCMC (20+), Da Nang (8+), with long tail
    - Clinic type distribution: ~40% Phong kham, ~30% Nha thuoc, ~20% Trai chan nuoi
    - data/seeds/conversations.ts exports `generateConversations(profiles)` function
    - Function returns 10,000-12,000 conversation objects
    - Conversations have UUID format starting with `cv`
    - created_at dates span Jan 2024 - Mar 2026
    - Monthly volume follows the specified growth curve (low in 2024, peak in 2025 Q3-Q4)
    - Both generators use deterministic hash for reproducibility
  </acceptance_criteria>
  <done>Profile generator produces 130+ users; conversation generator produces 10K-12K conversations with realistic growth curve</done>
</task>

<task type="auto">
  <name>Task 2: Create message, chat_analytics, and query_events generators</name>
  <files>data/seeds/messages.ts, data/seeds/chat_analytics.ts, data/seeds/query_events.ts</files>
  <read_first>
    - data/seeds/conversations.ts (created in Task 1 -- understand ConversationSeed type and output)
    - data/seeds/query_events.md (current query_events format)
    - scripts/seed.ts (understand how these generators will be consumed)
  </read_first>
  <action>
**data/seeds/messages.ts:**

Create generator: `export function generateMessages(conversations: ConversationSeed[]): MessageSeed[]`

1. For each conversation, generate 3-8 messages (deterministic based on conversation ID hash)
   - Average ~5 messages per conversation -> total 50,000-60,000 messages
2. Message pattern: alternating user/assistant roles
   - First message: always 'user' role
   - Last message: always 'assistant' role
   - Messages between: alternating user/assistant
3. Content: generated from templates based on the conversation's drug_group/animal_type
   - User messages: questions about drug dosage, disease symptoms, treatment plans (Vietnamese)
   - Assistant messages: longer responses with drug recommendations, dosage info (Vietnamese)
   - User message length: 20-100 chars
   - Assistant message length: 100-500 chars
4. UUID format: `msXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX`
5. created_at: spaced 1-5 minutes apart within the conversation, starting from conversation.created_at

**data/seeds/chat_analytics.ts:**

Create generator: `export function generateChatAnalytics(conversations: ConversationSeed[]): ChatAnalyticsSeed[]`

1. One row per conversation (1:1 mapping)
2. drug_group distribution: khang_sinh 35%, vitamin 20%, vac_xin 18%, hormone 12%, khang_ky_sinh_trung 10%, khac 5%
3. animal_type distribution: trau_bo 30%, lon 25%, ga 20%, cho_meo 15%, thuy_san 7%, khac 3%
4. query_type distribution: dieu_tri 35%, chan_doan 28%, lieu_luong 20%, phong_benh 12%, khac 5%
5. Use deterministic hash on conversation ID to assign categories
6. created_at: same as conversation.created_at

**data/seeds/query_events.ts:**

Create generator: `export function generateQueryEvents(conversations: ConversationSeed[], profiles: ProfileSeed[]): QueryEventSeed[]`

1. One row per conversation (mirrors chat_analytics but with user geography)
2. Fields: user_id, conversation_id, drug_group, animal_type, query_type, province, clinic_type (from profile), year, month, day (from conversation.created_at)
3. This feeds the materialized views (mv_monthly_queries, mv_daily_queries, mv_category_stats)
4. UUID format: `qeXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX`

All three generators must use the deterministic hash pattern for reproducibility.
  </action>
  <verify>
    <automated>cd "D:/importantProjects/WorkSpace_Personal Project_ Agents/Bamboo Vet" && test -f data/seeds/messages.ts && test -f data/seeds/chat_analytics.ts && test -f data/seeds/query_events.ts && grep -q "generateMessages" data/seeds/messages.ts && grep -q "generateChatAnalytics" data/seeds/chat_analytics.ts && grep -q "generateQueryEvents" data/seeds/query_events.ts && echo "All generators exist"</automated>
  </verify>
  <acceptance_criteria>
    - data/seeds/messages.ts exports `generateMessages(conversations)` returning 50,000-60,000 message objects
    - Messages alternate user/assistant roles within each conversation
    - Message UUIDs start with `ms`
    - data/seeds/chat_analytics.ts exports `generateChatAnalytics(conversations)` returning 1 row per conversation
    - Drug group distribution approximately: khang_sinh 35%, vitamin 20%, vac_xin 18%
    - data/seeds/query_events.ts exports `generateQueryEvents(conversations, profiles)` returning 1 row per conversation
    - Query events include province and clinic_type from the user's profile
    - All generators use deterministic hash for reproducibility
    - Running generators twice produces identical output
  </acceptance_criteria>
  <done>Message, chat_analytics, and query_events generators produce realistic data matching conversation volume</done>
</task>

</tasks>

<verification>
- All 5 generator files exist in data/seeds/
- Each exports a named generator function
- Profile count: 120-155
- Conversation count: 10,000-12,000
- Message count: 50,000-60,000
- Chat analytics count: equals conversation count
- Query events count: equals conversation count
</verification>

<success_criteria>
1. Profile generator produces 120-150 users with proper geographic and clinic type distribution
2. Conversation generator produces 10K-12K conversations following the non-linear growth curve
3. Message generator produces 50K-60K messages with realistic conversation patterns
4. Chat analytics and query events match 1:1 with conversations
5. All generators are deterministic (same output on repeated runs)
</success_criteria>

<output>
After completion, create `.planning/phases/06-security-polish/06-05-SUMMARY.md`
</output>
