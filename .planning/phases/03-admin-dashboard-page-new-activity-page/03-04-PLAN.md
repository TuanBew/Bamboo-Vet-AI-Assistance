---
phase: 03-admin-dashboard-page-new-activity-page
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/admin/services/dashboard.ts
  - lib/admin/services/new-activity.ts
autonomous: true
gap_closure: true
requirements: [DASH-01, DASH-04, DASH-05, ACT-01, ACT-06]
must_haves:
  truths:
    - "Dashboard category_stats.drug_groups returns non-empty array when mv_category_stats has data"
    - "Dashboard per-user drug_group_breakdown populates from query_events (not mv_category_stats)"
    - "New Activity category_stats.drug_groups returns non-empty array when mv_category_stats has data"
  artifacts:
    - path: "lib/admin/services/dashboard.ts"
      provides: "Corrected fetchCategoryStats + per-user breakdown from query_events"
      contains: "drug_category"
    - path: "lib/admin/services/new-activity.ts"
      provides: "Corrected category stats query"
      contains: "drug_category"
  key_links:
    - from: "lib/admin/services/dashboard.ts"
      to: "mv_category_stats"
      via: "Supabase select"
      pattern: "select.*drug_category.*animal_type.*query_type.*count"
    - from: "lib/admin/services/dashboard.ts"
      to: "query_events"
      via: "Supabase select for per-user breakdown"
      pattern: "from.*query_events"
    - from: "lib/admin/services/new-activity.ts"
      to: "mv_category_stats"
      via: "Supabase select"
      pattern: "select.*drug_category.*animal_type.*query_type.*count"
---

<objective>
Fix schema column mismatches between dashboard/new-activity services and the mv_category_stats materialized view. The services query columns `drug_group`, `query_count`, and `user_id` that do not exist in the view — the actual columns are `drug_category`, `count`, and there is no `user_id`.

Purpose: Category donut charts (6 on dashboard, 3 on new-activity) and per-user drug/query-type inline BarCharts currently return empty data at runtime due to these mismatches.

Output: Both service files corrected to use actual view column names; per-user category breakdown queries `query_events` directly instead of `mv_category_stats`.
</objective>

<execution_context>
@C:/Users/MSI/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MSI/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-dashboard-page-new-activity-page/03-VERIFICATION.md

<interfaces>
<!-- mv_category_stats actual columns (from migration 006): -->
<!-- year (int), month (int), province (text), clinic_type (text), drug_category (text), animal_type (text), query_type (text), count (bigint) -->
<!-- NOTE: No user_id column exists in this view -->

<!-- query_events actual columns (from migration 003): -->
<!-- id (uuid), user_id (uuid), conversation_id (uuid), clinic_id (uuid), drug_category (text), animal_type (text), query_type (text), response_time_ms (int), created_at (timestamptz) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dashboard.ts — mv_category_stats column names + per-user breakdown from query_events</name>
  <files>lib/admin/services/dashboard.ts</files>
  <read_first>
    - lib/admin/services/dashboard.ts (full file — current broken state)
    - supabase/migrations/20260318_006_create_materialized_views.sql (mv_category_stats actual columns)
    - supabase/migrations/20260318_003_create_query_events.sql (query_events actual columns)
  </read_first>
  <action>
Three fixes in `lib/admin/services/dashboard.ts`:

**Fix 1 — fetchCategoryStats function (around line 208-244):**

1. Line 210: Change `.select('drug_group, animal_type, query_type, query_count, user_id')` to `.select('drug_category, animal_type, query_type, count, province, clinic_type')`

2. Remove the `filteredUserIds` parameter and the `.in('user_id', filteredUserIds)` filter (lines 214-216). Instead, apply province and clinic_type filters directly on the view:
   - If the caller passes `province`, add `.eq('province', province)`
   - If the caller passes `clinic_type`, add `.eq('clinic_type', clinic_type)`
   - Update the function signature to accept `province?: string, clinicType?: string` instead of `filteredUserIds: string[] | null`

3. Line 225: Change `Number(row.query_count)` to `Number(row.count)`

4. Line 226: Change `row.drug_group || 'Khac'` to `row.drug_category || 'Khac'`

5. Update the caller of `fetchCategoryStats` in the main `getDashboardData` function to pass `province` and `clinicType` instead of `filteredUserIds`.

**Fix 2 — Per-user category breakdown (around line 370-394):**

The block at lines 370-394 queries `mv_category_stats` with `.in('user_id', top20Ids)` — but `mv_category_stats` has no `user_id` column.

Replace this query to use `query_events` directly:

```typescript
const { data: catData } = await db
  .from('query_events')
  .select('user_id, drug_category, query_type')
  .in('user_id', top20Ids)
```

Then update the loop (lines 379-394):
- Line 381: Change `Number(row.query_count)` to `1` (each query_events row = 1 event)
- Line 384: Change `row.drug_group || 'Khac'` to `row.drug_category || 'Khac'`
- Line 390: Change `row.query_type || 'Khac'` stays as-is (query_type column name is correct in query_events)

Also update the type interface if `drug_group_breakdown` is referenced in the return type — the field name `drug_group_breakdown` in the return object (line 420) can stay as-is since it is the API response field name, not a database column name.

**Fix 3 — Update getDashboardData function signature/call:**

Find where `fetchCategoryStats` is called in `getDashboardData`. It currently passes `filteredUserIds`. Change it to pass the raw `province` and `clinicType` filter strings from the request params.
  </action>
  <verify>
    <automated>cd "D:\importantProjects\WorkSpace_Personal Project_ Agents\Bamboo Vet" && npx tsc --noEmit lib/admin/services/dashboard.ts 2>&1 | head -20 || npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "drug_category" lib/admin/services/dashboard.ts returns >= 3 (select + two row access lines)
    - grep -c "drug_group" lib/admin/services/dashboard.ts returns 2 or fewer (only the response field names like drug_group_breakdown and drug_groups, NOT database column references)
    - grep "from.*query_events" lib/admin/services/dashboard.ts matches at least 1 line (per-user breakdown query)
    - grep "query_count.*user_id" lib/admin/services/dashboard.ts returns 0 matches (removed wrong column combo)
    - grep "\.select.*drug_category.*count" lib/admin/services/dashboard.ts matches the fetchCategoryStats select
    - npm run build completes without TypeScript errors
  </acceptance_criteria>
  <done>fetchCategoryStats queries mv_category_stats using correct column names (drug_category, count) with province/clinic_type filters instead of user_id filter. Per-user breakdown queries query_events directly. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Fix new-activity.ts — mv_category_stats column name drug_group to drug_category</name>
  <files>lib/admin/services/new-activity.ts</files>
  <read_first>
    - lib/admin/services/new-activity.ts (full file — current broken state)
    - supabase/migrations/20260318_006_create_materialized_views.sql (mv_category_stats actual columns)
  </read_first>
  <action>
Two changes in `lib/admin/services/new-activity.ts`:

1. Line 279: Change `.select('drug_group, animal_type, query_type, count')` to `.select('drug_category, animal_type, query_type, count')`

2. Line 289: Change `row.drug_group` cast/access to `row.drug_category`:
   - Before: `const dg = (row.drug_group as string) || 'Khac'`
   - After:  `const dg = (row.drug_category as string) || 'Khac'`

No other changes needed — the `count` column name is already correct in new-activity.ts, and there is no `user_id` reference for mv_category_stats in this file.
  </action>
  <verify>
    <automated>cd "D:\importantProjects\WorkSpace_Personal Project_ Agents\Bamboo Vet" && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "drug_category" lib/admin/services/new-activity.ts matches at least 2 lines (select + row access)
    - grep "drug_group" lib/admin/services/new-activity.ts returns 0 matches (all references removed — the response field name "drug_groups" is built from the Map variable drugGroups, not from row.drug_group)
    - npm run build completes without TypeScript errors
  </acceptance_criteria>
  <done>new-activity.ts category stats query uses correct column name drug_category. Category donut charts will receive populated data at runtime.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run build` passes with no TypeScript errors
2. `grep -rn "drug_group" lib/admin/services/dashboard.ts` shows only response field names (drug_group_breakdown, drug_groups), never database column references in .select() or row.drug_group
3. `grep -rn "drug_group" lib/admin/services/new-activity.ts` returns 0 matches
4. `grep -rn "drug_category" lib/admin/services/dashboard.ts` shows at least 3 occurrences
5. `grep -rn "drug_category" lib/admin/services/new-activity.ts` shows at least 2 occurrences
6. `grep -rn "query_events" lib/admin/services/dashboard.ts` shows the per-user breakdown query
</verification>

<success_criteria>
- Both service files query mv_category_stats using actual column names: drug_category, count (not drug_group, query_count)
- Dashboard per-user category breakdown queries query_events directly (which has user_id) instead of mv_category_stats (which does not)
- No user_id reference in any mv_category_stats query
- Build passes without TypeScript errors
- All category stats (drug_groups, animal_types, query_types) arrays will be populated at runtime when mv_category_stats contains data
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-dashboard-page-new-activity-page/03-04-SUMMARY.md`
</output>
